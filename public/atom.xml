<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>OhYoooo</title>
  
  <subtitle>coding | photography | にじげん</subtitle>
  <link href="/atom.xml" rel="self"/>
  
  <link href="https://ohyoooo.github.io/"/>
  <updated>2019-10-14T10:11:33.478Z</updated>
  <id>https://ohyoooo.github.io/</id>
  
  <author>
    <name>OhYoo</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>Configure Kerberos Authentication in Golang</title>
    <link href="https://ohyoooo.github.io/2019/10/10/Configure-Kerberos-Authentication-in-Golang/"/>
    <id>https://ohyoooo.github.io/2019/10/10/Configure-Kerberos-Authentication-in-Golang/</id>
    <published>2019-10-10T10:54:37.000Z</published>
    <updated>2019-10-14T10:11:33.478Z</updated>
    
    <content type="html"><![CDATA[<p>Configure Kerberos authentication in Golang<br><a id="more"></a></p><h2 id="Breif-introduction-of-Kerberos"><a href="#Breif-introduction-of-Kerberos" class="headerlink" title="Breif introduction of Kerberos"></a>Breif introduction of Kerberos</h2><h2 id="Client"><a href="#Client" class="headerlink" title="Client"></a>Client</h2><p>There are multipe clients available for Golang, and you can refer to <a href="https://cwiki.apache.org/confluence/display/KAFKA/Clients" target="_blank" rel="noopener">here</a> to have a breif look. Usually we intent to choose libraries which are purely coded in the sme programming language to avoid importing unnecessary dependencies. Unfortunately, by the time I wrote this article, I haven’t found out a library which is purely written by Go, therefore I choose a cgo library supported by <a href="https://github.com/confluentinc/confluent-kafka-go" target="_blank" rel="noopener">Confluent</a>, this library refers to a C library <a href="https://github.com/edenhill/librdkafka" target="_blank" rel="noopener">librdkafka</a>.</p><h2 id="Environment"><a href="#Environment" class="headerlink" title="Environment"></a>Environment</h2><p>For the reason that I couldn’t refresh my keytab on MacOS (most likely because macos disable UDP connection by default and hence cannot recognize kdc in my realms), I switched to linux (for dockerfile, linux is also a good choice :P).</p><p>The Go version is required to be at least <code>1.12</code></p><p>Firstly, since our client refering to <code>librdkafka</code>, we need to install librdkafka:</p><pre><code>wget -qO - https://packages.confluent.io/deb/5.3/archive.key | apt-key add - &amp;&amp; \add-apt-repository &quot;deb [arch=amd64] https://packages.confluent.io/deb/5.3 stable main&quot;</code></pre><p>Then install:</p><pre><code>apt-get install -y librdkafka-dev</code></pre><p>I am going to use <code>GSSAPI</code> as <code>SASL</code> authentication and kerberos <code>krb</code> configuration hence need to import a few more tools:</p><pre><code>apt-get install -y libsasl2-modules-gssapi-mit libsasl2-devapt-get install -yqq krb5-user libpam-krb5</code></pre><p><code>libsasl2-modules-gssapi-mit</code> and <code>libsasl2-dev</code> are specified to GSSAPI authentication, if you are using other mechanism (for example PLAIN or SCRAM-SHA-256), please refer to corresponding tools.</p><p>And double check if you have <code>ca-certificates</code> installed (Not sure why, but without ca-certificates, krb configuration cannot be set)</p><h2 id="Create-a-Client"><a href="#Create-a-Client" class="headerlink" title="Create a Client"></a>Create a Client</h2><pre><code class="go">import kafka &quot;github.com/confluentinc/confluent-kafka-go/kafka&quot;client, err := kafka.NewConsumer(&amp;kafka.ConfigMap{    // Avoid connecting to IPv6 brokers:    // when using localhost brokers on OSX, since the OSX resolver    // will return the IPv6 addresses first.    // You typically don&#39;t need to specify this configuration property.    // &quot;broker.address.family&quot;:      &quot;v4&quot;,    &quot;bootstrap.servers&quot;:          &quot;[Server host:port]&quot;,    &quot;group.id&quot;:                   &quot;[Group id]&quot;,    &quot;security.protocol&quot;:          &quot;SASL_PLAINTEXT&quot;,    &quot;session.timeout.ms&quot;:         6000,    &quot;sasl.mechanism&quot;:             &quot;GSSAPI&quot;,    &quot;auto.offset.reset&quot;:          &quot;earliest&quot;,    &quot;sasl.kerberos.service.name&quot;: &quot;[Service name]&quot;,    &quot;sasl.kerberos.keytab&quot;:       &quot;[Key tab location]&quot;,    &quot;sasl.kerberos.principal&quot;:    &quot;[Principal]&quot;,    &quot;sasl.kerberos.kinit.cmd&quot;:    &quot;kinit -R -t \&quot;%{sasl.kerberos.keytab}\&quot; -k %{sasl.kerberos.principal}&quot;,})Then export [JAAS configurations](https://docs.confluent.io/current/kafka/authentication_sasl/index.html)</code></pre><p>export KRB5_CONFIG=”/usr/local/kafka/conf/krb5/krb5.conf”<br>export KAFKA_OPTS=”-Djava.security.auth.login.config=/usr/local/kafka/conf/kafka/kafka_client_jaas.conf-Djava.security.krb5.conf=/usr/local/kafka/conf/krb5/krb5.conf -Dsun.security.krb5.debug=true”</p><pre><code>For here, I use `keytab` to authorize which need to configured on kafka server side. Initially, we can use `username` and `password` to authorize.</code></pre><pre><code>&quot;sasl.username&quot;: username,&quot;sasl.password&quot;: password</code></pre><pre><code>Other configuration fields can be referred to [here](https://github.com/edenhill/librdkafka/blob/master/CONFIGURATION.md)## DockerfileWell, if you are a lazy person like me, here is the dockerfile :D</code></pre><h1 id="refer-to-a-cgo-library-maintained-by-Confluent-https-github-com-confluentinc-confluent-kafka-go"><a href="#refer-to-a-cgo-library-maintained-by-Confluent-https-github-com-confluentinc-confluent-kafka-go" class="headerlink" title="refer to a cgo library maintained by Confluent: https://github.com/confluentinc/confluent-kafka-go"></a>refer to a cgo library maintained by Confluent: <a href="https://github.com/confluentinc/confluent-kafka-go" target="_blank" rel="noopener">https://github.com/confluentinc/confluent-kafka-go</a></h1><h1 id="which-requires-a-C-dependency-librdkafka-dev-https-github-com-edenhill-librdkafka"><a href="#which-requires-a-C-dependency-librdkafka-dev-https-github-com-edenhill-librdkafka" class="headerlink" title="which requires a C dependency librdkafka-dev: https://github.com/edenhill/librdkafka"></a>which requires a C dependency librdkafka-dev: <a href="https://github.com/edenhill/librdkafka" target="_blank" rel="noopener">https://github.com/edenhill/librdkafka</a></h1><h1 id="The-C-dependency-librdkafka-dev-is-curretly-not-available-for-other-linux-version-except-for-ubuntu-debian"><a href="#The-C-dependency-librdkafka-dev-is-curretly-not-available-for-other-linux-version-except-for-ubuntu-debian" class="headerlink" title="The C dependency librdkafka-dev is curretly not available for other linux version except for ubuntu/debian."></a>The C dependency librdkafka-dev is curretly not available for other linux version except for ubuntu/debian.</h1><p>FROM ubuntu</p><p>ENV http_proxy=<br>ENV https_proxy=</p><p>ENV DEBIAN_FRONTEND=noninteractive</p><h1 id="Install-the-C-lib-for-kafka"><a href="#Install-the-C-lib-for-kafka" class="headerlink" title="Install the C lib for kafka"></a>Install the C lib for kafka</h1><p>RUN apt-get update &amp;&amp; \<br>    apt-get install -y –no-install-recommends apt-utils wget gnupg software-properties-common &amp;&amp; \<br>    apt-get install -y apt-transport-https ca-certificates git curl openssl libsasl2-modules-gssapi-mit libsasl2-dev &amp;&amp; \<br>    apt-get install -yqq krb5-user libpam-krb5 &amp;&amp; \</p><h1 id="import-source-repository-from-confluent-check-the-latest-version-on-https-docs-confluent-io-current-installation-installing-cp-deb-ubuntu-html-get-the-software"><a href="#import-source-repository-from-confluent-check-the-latest-version-on-https-docs-confluent-io-current-installation-installing-cp-deb-ubuntu-html-get-the-software" class="headerlink" title="import source repository from confluent, check the latest version on https://docs.confluent.io/current/installation/installing_cp/deb-ubuntu.html#get-the-software"></a>import source repository from confluent, check the latest version on <a href="https://docs.confluent.io/current/installation/installing_cp/deb-ubuntu.html#get-the-software" target="_blank" rel="noopener">https://docs.confluent.io/current/installation/installing_cp/deb-ubuntu.html#get-the-software</a></h1><pre><code>wget -qO - https://packages.confluent.io/deb/5.3/archive.key | apt-key add - &amp;&amp; \add-apt-repository &quot;deb [arch=amd64] https://packages.confluent.io/deb/5.3 stable main&quot; &amp;&amp; \</code></pre><h1 id="import-the-librdkafka-dev-from-confluent-source-repository"><a href="#import-the-librdkafka-dev-from-confluent-source-repository" class="headerlink" title="import the librdkafka-dev from confluent source repository"></a>import the librdkafka-dev from confluent source repository</h1><h1 id="confluent-kafka-go-always-requires-the-latest-librdkafka-dev-library"><a href="#confluent-kafka-go-always-requires-the-latest-librdkafka-dev-library" class="headerlink" title="confluent-kafka-go always requires the latest librdkafka-dev library"></a>confluent-kafka-go always requires the latest librdkafka-dev library</h1><h1 id="If-go-build-fail-below-because-of-the-mismatch-of-confluent-kafka-go-and-librdkafka-dev"><a href="#If-go-build-fail-below-because-of-the-mismatch-of-confluent-kafka-go-and-librdkafka-dev" class="headerlink" title="If go build fail below because of the mismatch of confluent-kafka-go and librdkafka-dev,"></a>If go build fail below because of the mismatch of confluent-kafka-go and librdkafka-dev,</h1><h1 id="please-check-the-latest-source-repositary-on-https-docs-confluent-io-current-installation-installing-cp-deb-ubuntu-html-get-the-software"><a href="#please-check-the-latest-source-repositary-on-https-docs-confluent-io-current-installation-installing-cp-deb-ubuntu-html-get-the-software" class="headerlink" title="please check the latest source repositary on https://docs.confluent.io/current/installation/installing_cp/deb-ubuntu.html#get-the-software"></a>please check the latest source repositary on <a href="https://docs.confluent.io/current/installation/installing_cp/deb-ubuntu.html#get-the-software" target="_blank" rel="noopener">https://docs.confluent.io/current/installation/installing_cp/deb-ubuntu.html#get-the-software</a></h1><pre><code>apt-get install -y librdkafka-dev &amp;&amp; \</code></pre><h1 id="Install-Go"><a href="#Install-Go" class="headerlink" title="Install Go"></a>Install Go</h1><pre><code>add-apt-repository ppa:longsleep/golang-backports &amp;&amp; \apt-get install -y golang-1.12-go</code></pre><h1 id="build-the-library"><a href="#build-the-library" class="headerlink" title="build the library"></a>build the library</h1><p>WORKDIR /src</p><p>ADD . /src</p><p>RUN GOPATH=/go GOOS=linux /usr/lib/go-1.12/bin/go build -o app &amp;&amp; \<br>    mv /src/app /usr/local/bin</p><p>ENV http_proxy ‘’<br>ENV https_proxy ‘’</p><p>EXPOSE 8000</p><p>ENTRYPOINT [“/usr/local/bin/app”]</p><p><code>`</code></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;Configure Kerberos authentication in Golang&lt;br&gt;&lt;/p&gt;
    
    </summary>
    
    
      <category term="Kafka" scheme="https://ohyoooo.github.io/tags/Kafka/"/>
    
      <category term="Golang" scheme="https://ohyoooo.github.io/tags/Golang/"/>
    
      <category term="Kerberos" scheme="https://ohyoooo.github.io/tags/Kerberos/"/>
    
  </entry>
  
  <entry>
    <title>A Brew Error When Upgrade Mongodb to 4.2</title>
    <link href="https://ohyoooo.github.io/2019/10/01/An-Brew-Error-When-Upgrade-Mongodb-to-4-2/"/>
    <id>https://ohyoooo.github.io/2019/10/01/An-Brew-Error-When-Upgrade-Mongodb-to-4-2/</id>
    <published>2019-10-01T08:38:05.000Z</published>
    <updated>2019-10-14T10:11:33.477Z</updated>
    
    <content type="html"><![CDATA[<p>When I tried to upgrade my old mongodb to 4.2 through <code>homebrew</code>:</p><pre><code>brew tap mongodb/brew</code></pre><a id="more"></a><p>then:</p><pre><code>brew install mongodb-community@4.2</code></pre><p>I encountered a strange error saying:</p><pre><code>Error: parent directory is world writable but not sticky</code></pre><p>That is because of the privilege issue of writing to /tmp,<br>do the following will solve the problem:</p><pre><code>sudo chmod +t /private/tmp</code></pre><p>Remeber to link to the new version after the installation:</p><pre><code>brew link --overwrite mongodb-community</code></pre>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;When I tried to upgrade my old mongodb to 4.2 through &lt;code&gt;homebrew&lt;/code&gt;:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;brew tap mongodb/brew
&lt;/code&gt;&lt;/pre&gt;
    
    </summary>
    
    
      <category term="Brew" scheme="https://ohyoooo.github.io/tags/Brew/"/>
    
      <category term="Mongo" scheme="https://ohyoooo.github.io/tags/Mongo/"/>
    
  </entry>
  
  <entry>
    <title>Hello World</title>
    <link href="https://ohyoooo.github.io/2019/07/31/hello-world/"/>
    <id>https://ohyoooo.github.io/2019/07/31/hello-world/</id>
    <published>2019-07-31T08:54:49.514Z</published>
    <updated>2019-07-31T08:54:49.514Z</updated>
    
    <content type="html"><![CDATA[<p>Welcome to <a href="https://hexo.io/" target="_blank" rel="noopener">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/" target="_blank" rel="noopener">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html" target="_blank" rel="noopener">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues" target="_blank" rel="noopener">GitHub</a>.<a id="more"></a></p><h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><pre><code class="bash">$ hexo new &quot;My New Post&quot;</code></pre><p>More info: <a href="https://hexo.io/docs/writing.html" target="_blank" rel="noopener">Writing</a></p><h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><pre><code class="bash">$ hexo server</code></pre><p>More info: <a href="https://hexo.io/docs/server.html" target="_blank" rel="noopener">Server</a></p><h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><pre><code class="bash">$ hexo generate</code></pre><p>More info: <a href="https://hexo.io/docs/generating.html" target="_blank" rel="noopener">Generating</a></p><h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><pre><code class="bash">$ hexo deploy</code></pre><p>More info: <a href="https://hexo.io/docs/deployment.html" target="_blank" rel="noopener">Deployment</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;Welcome to &lt;a href=&quot;https://hexo.io/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Hexo&lt;/a&gt;! This is your very first post. Check &lt;a href=&quot;https://hexo.io/docs/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;documentation&lt;/a&gt; for more info. If you get any problems when using Hexo, you can find the answer in &lt;a href=&quot;https://hexo.io/docs/troubleshooting.html&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;troubleshooting&lt;/a&gt; or you can ask me on &lt;a href=&quot;https://github.com/hexojs/hexo/issues&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;GitHub&lt;/a&gt;.&lt;/p&gt;
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>Suffix Tree</title>
    <link href="https://ohyoooo.github.io/2019/07/22/Suffix-Tree/"/>
    <id>https://ohyoooo.github.io/2019/07/22/Suffix-Tree/</id>
    <published>2019-07-22T11:07:12.000Z</published>
    <updated>2019-08-03T08:38:23.297Z</updated>
    
    <content type="html"><![CDATA[<p>Suffix tree is a data structure aimming at solving <strong>string</strong> related problems in <strong>linear time</strong><br><a id="more"></a></p><h2 id="String-Match-Algorithm"><a href="#String-Match-Algorithm" class="headerlink" title="String Match Algorithm"></a>String Match Algorithm</h2><p>A common string match problem always contains:</p><ul><li><strong>Text</strong> as an array of length n: T[n]</li><li><strong>Pattern</strong> as an array of length m which m &lt;=n: P[m]</li><li><strong>Valid Shift</strong> which is the offset of the first character of the pattern showing up in the target text<br><img src="/2019/07/22/Suffix-Tree/PrefixTree_StringProblem.JPG" alt="String Match Problem Sample"></li></ul><p>We usually use two methods to solve string match problem:</p><ul><li>Naive String Matching Algorithm</li><li>Knuth-Morris-Pratt Algorithm（KMP Algorithm)</li></ul><p>The algorithm always consists with two steps: <strong>Preprocessing</strong> and <strong>Matching</strong>, and the total runtime will be accordingly the sum of two procedures.</p><blockquote><p>Naive String Matching: O( (n-m)m )<br>KMP: O( m + n )</p></blockquote><p>Such algorithms are all doing preprocessings on the pattern to boost the searching procedure and the best runtime perfomance will be O(m) on preprocessing where m represents the length of the pattern. On contrary, is there a preprocessing which can be applied on <strong>text</strong> to speed up the whole process? This is the key reason why I am moving to <strong>suffix tree</strong>, which is a data structure doing preprocessing on text.<br><br></p><h2 id="Suffix-Tree"><a href="#Suffix-Tree" class="headerlink" title="Suffix Tree"></a>Suffix Tree</h2><p>My previous post used to introduce a prefix tree, for example:<br><img src="/2019/07/22/Suffix-Tree/prefix_tree.JPG" alt="prefix tree sample"><br>Individual nodes branch out from the same prefix. As you can see, there are some nodes which only have one child. Let’s try to compress them together:<br><img src="/2019/07/22/Suffix-Tree/Compressed_PrefixTree.JPG" alt="prefix tree sample"><br>After the compression, we get a <strong>Compressed Prefix Tree</strong>. A compressed prefix tree, also called <strong>Compressed Tire</strong> is the fundamental of suffix tree. Besides, the key values stored in each nodes of a suffix tree is all the possible suffix.</p><p>For example, for a single word (<strong>Text</strong>) <code>banana\0</code>, we have the following set of suffix:</p><pre><code>banana\0anana\0nana\0ana\0na\0a\0\0</code></pre><p>Construct a prefix tree using the above key words:<br><img src="/2019/07/22/Suffix-Tree/Banana_PrefixTree.JPG" alt="Banana Prefix Tree"><br>Afterwards, compress it:<br><img src="/2019/07/22/Suffix-Tree/Banana_SuffixTree.JPG" alt="Banana Suffix Tree"><br>Here, by lising all the suffix and making a compressed prefix tree, we obtain a suffix tree.</p><p>However, there is a faster way to construct a suffix tree which can be done in the linear time (By Esko Ukkonen in 1997). Let’s start from a simple example before exploring more complicated cases.<br><br></p><h2 id="“abc”"><a href="#“abc”" class="headerlink" title="“abc”"></a>“abc”</h2><p>Unlike prefix tree, the edge in suffix tree will no longer represent a single character(key); instead, it will represent a list of integers [from, to] which interprets the indexes in the original text. Therefore, each edge can represent abitrary length of characters and consume constant memory.</p><p>Considering a simple string <code>abc</code> which has no duplicated character. Start from the first character <code>a</code>, construct an edge from root node to its leaf:<br><img src="/2019/07/22/Suffix-Tree/PrefixTree_a.JPG" alt="Sample &quot;abc&quot; -- a"><br>Where [0, #] represents start from offset 0 and end up at offset #. Currently # equals to 1, which represents character <code>a</code>.</p><p>After finishing the procudure of the first character, let’s move forward to the second one <code>b</code>:<br><img src="/2019/07/22/Suffix-Tree/PrefixTree_ab.JPG" alt="Sample &quot;abc&quot; -- ab"><br>Then do the same thing on the last character <code>c</code>:<br><img src="/2019/07/22/Suffix-Tree/PrefixTree_abc.JPG" alt="Sample &quot;abc&quot; -- abc"><br>We maintain a pointer pointing to the current end character of the text. Each time when we add a new edge for a new character, we will update the # point. Thus, the runtime for each procedure is O(1) and hence the overall runtime to construct this “abc” suffix tree will be O(n).<br><br></p><h2 id="“abcabxabcd”"><a href="#“abcabxabcd”" class="headerlink" title="“abcabxabcd”"></a>“abcabxabcd”</h2><p>“abc” is a really simple example since there is no duplicated characters in the text. Now let’s consider a more complicated text <code>abcabxabcd</code>. Before we get started to construct, there are two more concepts that need to be introduced</p><ul><li><strong>active point</strong>: a structure indicates <code>active_node</code>, <code>active_value</code> and <code>offset</code></li><li><strong>remainder</strong>: remaining characters to be inserted</li></ul><pre><code>active_point = (root, &#39;&#39;, 0)remainder = 0</code></pre><p><br></p><h3 id="The-first-character-gt-a"><a href="#The-first-character-gt-a" class="headerlink" title="The first character =&gt; a"></a>The first character =&gt; <code>a</code></h3><p>We append <code>a</code> directly to the root node</p><ol><li>Before we append the node, set <code>remainder = remainder + 1</code> (0 + 1), states that we are going to insert a new node</li><li>Append ‘a’ to the root node</li><li>After the insertion, a new node is appended to the tree, hence <code>remainder = remainder - 1</code> (1 - 1)</li><li>After the whole procudure:<pre><code>active_point = (root, &#39;&#39;, 0)remainder = 0</code></pre></li></ol><p><img src="/2019/07/22/Suffix-Tree/SuffixTree-Sample-a.JPG" alt="Sample &quot;abcabx&quot; -- abc"></p><h3 id="The-second-character-gt-b"><a href="#The-second-character-gt-b" class="headerlink" title="The second character =&gt; b"></a>The second character =&gt; <code>b</code></h3><ol><li>Before we append the node, set <code>remainder = remainder + 1</code> (0 + 1), states that we are going to insert a new node</li><li>Expend every leave node; hence change ‘a’ to ‘ab’</li><li>Query the current node, which is the root node, since there is no prefix starts with ‘b’, we need to create a new node and insert ‘b’. (The prefix for the previous prefix will be [a, ab] now)</li><li>After the insertion, a new node is appended to the tree, hence <code>remainder = remainder - 1</code> (1 - 1)</li><li>After the whole procudure:<pre><code>active_point = (root, &#39;&#39;, 0)remainder = 0</code></pre></li></ol><p><img src="/2019/07/22/Suffix-Tree/SuffixTree-Sample-ab.JPG" alt="Sample &quot;abcabx&quot; -- abc"></p><h3 id="The-third-character-gt-c"><a href="#The-third-character-gt-c" class="headerlink" title="The third character =&gt; c"></a>The third character =&gt; <code>c</code></h3><p>Same as the procedure of inserting ‘b’<br><img src="/2019/07/22/Suffix-Tree/SuffixTree-Sample-abc.JPG" alt="Sample &quot;abcabx&quot; -- abc"></p><h3 id="The-fourth-character-gt-a-again"><a href="#The-fourth-character-gt-a-again" class="headerlink" title="The fourth character =&gt; a again"></a>The fourth character =&gt; <code>a</code> again</h3><p>This time we encounter the same character we have already inserted</p><ol><li>Before we append the node, set <code>remainder = remainder + 1</code> (0 + 1)</li><li>Expend every leave node; hence change ‘abc’ -&gt; ‘abca’, ‘bc’ -&gt; ‘bca’, ‘c’ -&gt; ‘ca’</li><li>Query the current node, which is the root node, find out the there already exist a prefix which starts from ‘a’; hence we modify the <code>active_point</code>:<pre><code>active_point = (root, &#39;a&#39;, 1)remainder = 1</code></pre><blockquote><p><strong>active_node</strong>: the parent node is still root node hence remain unchanged<br><strong>active_value</strong>: the current value will be set to ‘a’<br><strong>offset</strong>: the offset of the current value, which is 1 (states the first character in the prefix)<br><strong>remainder</strong> after the insertion, no new node is inserted in the tree, hence remainder keep the same (remainder = 1)</p></blockquote></li></ol><h3 id="The-fifth-character-gt-b-again"><a href="#The-fifth-character-gt-b-again" class="headerlink" title="The fifth character =&gt; b again"></a>The fifth character =&gt; <code>b</code> again</h3><p>Still duplicated with previous nodes</p><ol><li><code>remainder = remainder + 1</code> (1 + 1)</li><li>Expend every leave node; hence change ‘abca’ -&gt; ‘abcab’, ‘bca’ -&gt; ‘bcab’, ‘ca’ -&gt; ‘cab’, <code>active_point[1] = &#39;a&#39; -&gt; &#39;ab&#39;</code></li><li>Start from <strong>active_node</strong>, query the <strong>active_value</strong> in its prefix and the prefix of its sub-nodes. Modify <strong>active_point</strong> to the following:<pre><code>active_point = (root, &#39;ab&#39;, 2)remainder = 2</code></pre><blockquote><p><strong>active_node</strong>: the active node is still root node hence remain unchanged.<br><strong>active_value</strong>: the active value has been expended to ‘ab’.<br><strong>offset</strong>: ‘ab’ already exists in node ‘ab’ (which is the sub-node of root node), the offset of matched index is 2.<br><strong>remainder</strong>: after the insertion, no new node is inserted in the tree, hence remainder keep the same</p></blockquote></li></ol><p>By this step, we still have the same amount of nodes inserted into the tree (3 nodes)<br><img src="/2019/07/22/Suffix-Tree/SuffixTree-Sample-abcab.JPG" alt="Sample &quot;abcabx&quot; -- abcab"></p><h3 id="The-sixth-character-gt-x"><a href="#The-sixth-character-gt-x" class="headerlink" title="The sixth character =&gt; x"></a>The sixth character =&gt; <code>x</code></h3><ol><li><code>remainder = remainder + 1</code> (2 + 1).</li><li>Expend every leave node; hence change ‘abcab’ -&gt; ‘abcabx’, ‘bcab’ -&gt; ‘bcabx’, ‘cab’ -&gt; ‘cabx’, <code>active_point[1] = &#39;ab&#39; -&gt; &#39;abx&#39;</code>.</li><li>Start from <strong>active_node</strong>, query the <strong>active_value</strong> in its prefix and the prefix of its sub-nodes. Since there isn’t a prefix which perfectly matches the pattern ‘abx’; hence this is the point where to <strong>split</strong>:</li></ol><h3 id="Split"><a href="#Split" class="headerlink" title="Split"></a>Split</h3><p>When we find out the exact place where the prefix starts to mismatch the pattern, then we need to insert new nodes to our tree to append this new suffix character. For example, prefix ‘abc’ and pattern ‘abx’ share the same two characters ‘ab’ at the beginning and mismatch at the third character (offset = 3), then we can simply split at the third index to make a tree like ‘ab’ -&gt; [‘c’, ‘x’]. We will do the same thing here:</p><ol><li>We already know the offset (which is active_point[2]).</li><li>Split node <strong>‘abcabx’ to ‘ab’ -&gt; [‘cabx’, ‘x’]</strong>.</li><li>Modify <strong>active_point</strong> and <strong>remainder</strong>:<pre><code>active_point = (root, &#39;bx&#39;, 1)remainder = 2</code></pre><blockquote><p><strong>active_node</strong>: still root node<br><strong>active_value</strong>: ‘abx’ has been inserted, then we need to insert ‘bx’, then ‘x’ and so on so far<br><strong>offset</strong>: active_point[1] changes from ‘abx’ to ‘bx’, then the offset will also shift from 2 to 1<br><strong>remainder</strong>: a new node is inserted, hence <code>remainder = remainder - 1</code> (3 - 1)</p><blockquote><p><strong>Why do we change offset as well?</strong><br>This is actually a trick to reduce runtime. For each step of insertion, we will make an sub suffix tree by characters we already have (inserted). For example, if we have a string ‘abc’ and we have inserted the last character ‘c’ in the suffix tree, then all possible suffix of string (text) ‘abc’, which is ‘abc’, ‘bc’ and ‘c’ must exist in the suffix tree, either implicitly (have not splitted yet, like the fourth insertion above) or explicitly (has its own node). Hence, by inserting ‘ab +x’ and splitting the node, we know that ‘b +x’ must exist in the tree; therefore, we reduce the offset by 1 to state that the offset of the next split will start from the first character of the node ‘b…’.</p></blockquote></blockquote></li></ol><p><img src="/2019/07/22/Suffix-Tree/SuffixTree-Sample-abcabx.JPG" alt="Sample &quot;abcabx&quot; -- abcabx"></p><ol start="4"><li>The remainder is still greater than 0; hence we will keep inserting ‘bx’. Do the same thing here, start from the <strong>parent_node</strong>, query the sub prefix ‘bx’. Found ‘bx’ in the prefix ‘bcabx’; hence split the node <strong>‘bcabx’ to ‘b’ -&gt; [‘cabx’, ‘x’]</strong></li><li>Modify <strong>active_point</strong> and <strong>remainder</strong>:<pre><code>active_point = (root, &#39;x&#39;, 0)remainder = 1</code></pre><blockquote><p><strong>active_node</strong>: still root node<br><strong>active_value</strong>: ‘bx’ has been inserted, then we need to insert ‘x’<br><strong>offset</strong>: active_point[1] changes from ‘bx’ to ‘x’, then the offset will also shift from 1 to 0<br><strong>remainder</strong>: a new node is inserted, hence <code>remainder = remainder - 1</code> (2 - 1)</p></blockquote></li></ol><p><img src="/2019/07/22/Suffix-Tree/SuffixTree-Sample-abcabx-2.JPG" alt="Sample &quot;abcabx&quot; -- abcabx - 2"></p><ol start="6"><li>Since remainder is still greater than 0, we have to do another insertion to reduce it to zero. However, the character ‘x’ hasn’t been inserted before, then we append ‘x’ directly to the root node.</li><li>Modify <strong>active_point</strong> and <strong>remainder</strong>:<pre><code>active_point = (root, &#39;&#39;, 0)remainder = 0</code></pre><blockquote><p><strong>active_node</strong>: still root node<br><strong>active_value</strong>: the last character ‘x’ has been inserted, reduce to empty<br><strong>remainder</strong>: a new node is inserted, hence <code>remainder = remainder - 1</code> (1- 1)</p></blockquote></li></ol><p><img src="/2019/07/22/Suffix-Tree/SuffixTree-Sample-abcabx-3.JPG" alt="Sample &quot;abcabx&quot; -- abcabx - 3"></p><p><strong>Is there a relationship between two splitted node <code>ab</code> and <code>b</code>?</strong><br>When we separated ‘ab’ and ‘b’, we modified the previous ‘abcabx’ node first, then modified the second node ‘bcabx’. If we encounter another string starting with ‘abk’, then after ‘abk’ is inserted, we definately need to insert ‘bk’ as well. Repeating the same query will slow down the whole procedure; thus, we can add a link from ‘ab’ to ‘b’ to indicate that if an update is made in node ‘ab’, then another insertion will be done on the link target ‘b’ afterwards.<br><img src="/2019/07/22/Suffix-Tree/SuffixTree-Sample-abcabx-4.JPG" alt="Sample &quot;abcabx&quot; -- abcabx - 4"></p><h3 id="The-seventh-character-gt-a"><a href="#The-seventh-character-gt-a" class="headerlink" title="The seventh character =&gt; a"></a>The seventh character =&gt; <code>a</code></h3><p>Same as the fourth insertion ‘a’</p><pre><code>active_point = (root, &#39;a&#39;, 1)remainder = 1</code></pre><h3 id="The-eighth-character-b"><a href="#The-eighth-character-b" class="headerlink" title="The eighth character = b"></a>The eighth character = <code>b</code></h3><p>Same as the fifth insertion ‘b’</p><pre><code>active_point = (root, &#39;ab&#39;, 2)remainder = 2</code></pre><h3 id="The-ninth-character-gt-c"><a href="#The-ninth-character-gt-c" class="headerlink" title="The ninth character =&gt; c"></a>The ninth character =&gt; <code>c</code></h3><p>When we try to insert ‘c’</p><ol><li><code>remainder= remainder + 1</code> (3)</li><li>Expand every leave node<br><img src="/2019/07/22/Suffix-Tree/SuffixTree-Sample-abcabxabc.JPG" alt="Sample &quot;abcabxabc&quot; -- abcabxabc"></li><li>Query ‘abc’, find ‘abc’ under the parent node ‘ab’<blockquote><p><strong>Extra Procedure</strong>:<br><strong>active_point</strong>: The node ‘cabxabc’ constains the last character of the target prefix (‘abc’), whose parent node is ‘ab’; thus, set <code>active_point[0] = &#39;ab&#39;</code>.<br><strong>active_node</strong>: set <code>active_point[1] = &#39;abc&#39;</code>.<br><strong>offset</strong>: the offset of the character ‘c’ in its node string(‘cabxabc’) which is 1. Set <code>active_point[2] = 1</code><br><strong>remainder</strong>: After the modification, no new node is inserted, hence <strong>remainder</strong> keeps unchanged.</p></blockquote></li></ol><pre><code>active_point = (&#39;ab&#39;, &#39;abc&#39;, 1)remainder = 3</code></pre><h3 id="The-last-character-gt-d"><a href="#The-last-character-gt-d" class="headerlink" title="The last character =&gt; d"></a>The last character =&gt; <code>d</code></h3><p>We will use the link built-up in previous steps to simplify the updates.</p><ol><li><code>remainder = remainder + 1</code> (3 + 1)</li><li>Expand every leave nodes</li><li>Query the current parent node (‘ab’), find if there exist any prefix contains the target ‘abcd’? Since there isn’t any prefix match the pattern, then this will be a split point.</li></ol><h3 id="Split-1"><a href="#Split-1" class="headerlink" title="Split"></a>Split</h3><blockquote><p><strong>active_point[0] = ‘ab’</strong>: start from the curent parent node (‘ab’), looking into its sub nodes.<br><strong>active_point[1] = ‘abcd’</strong>: ‘d’ is inserted into the active value .<br><strong>active_point[2] = 1</strong>: parent node ‘ab’ already contains ‘ab’, and its sub-node ‘cabxabcd’ starts from the third character ‘c’ at offset 0. Then the second character of ‘cabxabcd’, which is ‘a’ mismatches the following target ‘d’; hence this is where to be splitted, at offset 1 (of ‘cabxabcd’).</p></blockquote><ol><li>Split <strong>‘cabxabcd’ to c -&gt; [abxabcd, d]</strong></li><li>Since a new node has been inserted, then <code>remainder = remainder - 1</code> (3). Remainder is still greater than 0, we still cannot stop here.<pre><code>active_point = (&#39;ab&#39;, &#39;abcd&#39;, 1)remainder = 3</code></pre></li></ol><p><img src="/2019/07/22/Suffix-Tree/SuffixTree-Sample-abcabxabc-10-1.JPG" alt="Sample &quot;abcabxabc&quot; -- 10 - 1"></p><ol start="3"><li><p>active_point[0] (‘ab’) has a link pointing to ‘b’. We change the active_point according to the link target.</p><blockquote><p><strong>active_point[0] = ‘b’</strong><br><strong>active_point[1] = ‘bcd’</strong><br><strong>active_point[2] remains unchange</strong></p></blockquote></li><li><p>start from active_point[0], query its sub-nodes. ‘b’ is the parent node and its sub-node ‘cabxabcd’ contains ‘c’ at offset 0 and mismatch at offset 1; hence we split its sub-node <strong>‘cabxabcd’ to ‘c’ -&gt; [abxabcd, d]</strong>. Since a new node is inserted, <code>remainder = remainder - 1</code> (2) and <code>active_point[1] = &#39;cd&#39;</code><br><img src="/2019/07/22/Suffix-Tree/SuffixTree-Sample-abcabxabc-10-2.JPG" alt="Sample &quot;abcabxabc&quot; -- 10 - 2"></p></li><li>At this point, <strong>active_node</strong> reduce to empty and does not have any link, then we reset **active_point[0] = ‘root’</li><li><p>Insert ‘cd’, start from the current node (active_point[0]), which is the ‘root’, query its sub-nose for active_point[1] (‘cd’). Node ‘cabxabcd’ contains target character ‘c’ and mismatches at offset 1. Split <strong>‘cabxabcd’ to c -&gt; [abxabcd, d]</strong>. <strong>remainder = remainder - 1</strong> (1).<br><img src="/2019/07/22/Suffix-Tree/SuffixTree-Sample-abcabxabc-10-3.JPG" alt="Sample &quot;abcabxabc&quot; -- 10 - 3"></p></li><li><p>Finally insert ‘d’ directly on root node and <code>remainder = remainder - 1</code> which is 0<br><img src="/2019/07/22/Suffix-Tree/SuffixTree-Sample-abcabxabc-10-4.JPG" alt="Sample &quot;abcabxabc&quot; -- 10 - 4"></p></li></ol>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;Suffix tree is a data structure aimming at solving &lt;strong&gt;string&lt;/strong&gt; related problems in &lt;strong&gt;linear time&lt;/strong&gt;&lt;br&gt;&lt;/p&gt;
    
    </summary>
    
    
      <category term="Suffix Tree" scheme="https://ohyoooo.github.io/tags/Suffix-Tree/"/>
    
  </entry>
  
  <entry>
    <title>Prefix Tree</title>
    <link href="https://ohyoooo.github.io/2019/07/18/Prefix-Tree/"/>
    <id>https://ohyoooo.github.io/2019/07/18/Prefix-Tree/</id>
    <published>2019-07-18T09:30:12.000Z</published>
    <updated>2019-10-14T10:11:33.480Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>A trie, also called digital tree, radix tree or prefix tree, is a kind of search tree—an ordered tree data structure used to store a dynamic set or associative array where the keys are usually strings. </p></blockquote><p>Each node of the tree represents a string (prefix) and has multiple child nodes. A prefix tree is always used as search prompt when users browse through the search engine. The runtime of each query operation is independent with the size of the prefix tree; instead, it is decided by the length of the query target. <a id="more"></a><br><br></p><h2 id="Sample"><a href="#Sample" class="headerlink" title="Sample"></a>Sample</h2><p>Here list an array of some random words:</p><blockquote><p>sin, sis, con, com, cin, cmd</p></blockquote><p>Then we can construct a prefix tree as following:<br><img src="/2019/07/18/Prefix-Tree/prefix_tree.JPG" alt="prefix tree sample"><br>During the query operation, there is no need to iterate the whole tree; instead, simply following each node and recursively going through its children will return the target or none if the target does not exist in the tree.<br><br></p><h2 id="Construct-A-Prefix-Tree"><a href="#Construct-A-Prefix-Tree" class="headerlink" title="Construct A Prefix Tree"></a>Construct A Prefix Tree</h2><p>There are multiple ways to form a prefix tree. The most popular method is to maintain three arrays in the instance to record end, path and next.</p><ul><li><p><strong>Node</strong></p><pre><code class="python">class TrieNode:  def __init__(self):      self.nodes = {}      self.count = 0      self.isword = False</code></pre></li><li><p><strong>CRUD</strong></p><pre><code class="python">class Trie:  def __init__(self):      &quot;&quot;&quot;      Initialize data structure.      &quot;&quot;&quot;      self.root = TrieNode()  def insert(self, word: str):      &quot;&quot;&quot;      Inserts a word into the trie.      :type word: str      :rtype: void      &quot;&quot;&quot;      curr = self.root      for char in word:          if char not in curr.nodes:              curr.nodes[char] = TrieNode()          curr.nodes[char].count += 1          curr = curr.nodes[char]      curr.isword = True  def query(self, target: str):      &quot;&quot;&quot;      Returns if the word is in the trie.      :type target: str      :rtype: bool      &quot;&quot;&quot;      curr = self.root      for char in target:          if char not in curr.nodes:              return False          curr = curr.nodes[char]      return curr.isword  def startWith(self, prefix: str):      &quot;&quot;&quot;      Returns the number of words in the trie that start with the given prefix.         :type prefix: str      :rtype: int      &quot;&quot;&quot;      curr = self.root      for char in prefix:          if char not in curr.nodes:              return 0          curr = curr.nodes[char]      return curr.count  def delete(self, target: str):      &quot;&quot;&quot;      Returns true if target exist and successfully delete from the trie.      :type target: str      :rtype: bool      &quot;&quot;&quot;      curr = self.root      for char in prefix:          if char not in curr.nodes:              return False          curr = curr.nodes[char]      if curr.isword:          curr.isword = False          return True      return False  def listAllMatches(self, prefix: str):      &quot;&quot;&quot;      Returns all words started with prefix      :param prefix:      :return: List[str]      &quot;&quot;&quot;      result = []      def recursiveQuery(node: TrieNode, path: str):          if not node.nodes:              result.append(path)          else:              for key in node.nodes.keys():                  recursiveQuery(node.nodes[key], path + key)      curr = self.root      for char in prefix:          if char not in curr.nodes:              return result          curr = curr.nodes[char]      recursiveQuery(&#39;&#39;)      return result</code></pre></li></ul>]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;A trie, also called digital tree, radix tree or prefix tree, is a kind of search tree—an ordered tree data structure used to store a dynamic set or associative array where the keys are usually strings. &lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;Each node of the tree represents a string (prefix) and has multiple child nodes. A prefix tree is always used as search prompt when users browse through the search engine. The runtime of each query operation is independent with the size of the prefix tree; instead, it is decided by the length of the query target.&lt;/p&gt;
    
    </summary>
    
    
      <category term="Prefix Tree" scheme="https://ohyoooo.github.io/tags/Prefix-Tree/"/>
    
  </entry>
  
  <entry>
    <title>Implement Zero Data Loss in Spark Streaming</title>
    <link href="https://ohyoooo.github.io/2019/05/22/Implement-Zero-Data-Loss-in-Spark-Streaming/"/>
    <id>https://ohyoooo.github.io/2019/05/22/Implement-Zero-Data-Loss-in-Spark-Streaming/</id>
    <published>2019-05-22T09:19:14.000Z</published>
    <updated>2019-07-31T08:54:49.505Z</updated>
    
    <content type="html"><![CDATA[<p>This is a log from my own experience in spark streaming during my work. Base on different environment and servers, the strategy may vary. For here, I am working with <strong>Kafka</strong> and Google Cloud <strong>PubSub</strong>. <a id="more"></a></p><h2 id="Background"><a href="#Background" class="headerlink" title="Background"></a>Background</h2><p>I have such workflow that the spark streaming receives dataset from both Kafka and PubSub, after doing some clean up and modeling, push the data to the Cloud Datastore.<br><img src="/2019/05/22/Implement-Zero-Data-Loss-in-Spark-Streaming/sparkworkflow.JPG" alt="my Workflow"><br>It works fine when the data stream is small and reports me the expected values; however while the number of end users is growing large, especially when the data stream turns to be erratic and sometimes considerably large if end users interact frequently with our UI pages, the spark streaming will get a lot of uncertain runtime errors. Those errors are most likely caused by the finite number of workers in our cluster. Also when there are too much stream rushing over to the server, the limited memory in cache will cause failures. After we upgraded the cluster on cloud, the number of errors is significantly reduced.<br><img src="/2019/05/22/Implement-Zero-Data-Loss-in-Spark-Streaming/cpudiagram.JPG" alt="CPU Utilization"><br>But the data processed during the errors was permanently lost and cannot be recovered. The data running on spark is buffered in memory (cache) and will be cleared meanwhile a failure occurs. This is not desired since some valuable KPIs may be lost as well. To prevent such data loss, I tried different strategies.<br><br></p><h2 id="Checkpoint"><a href="#Checkpoint" class="headerlink" title="Checkpoint"></a>Checkpoint</h2><p>Checkpointing is a process supported by spark streaming after version which will save RDDs in log after being checkpointed. There are two level of checkpoints: reliable and local. <strong>Reliable checkpoint</strong> ensures that the data is stored permanentlly on HDFS, S3 or other distributed filesystems. Each job on cluster will create a directory for saving checkpointed logs, which will look pretty much like the directory below:</p><pre><code>├── &quot;SomeUUID&quot;│   └── rdd-#│       ├── part-timestamp1│       ├── .part-timestamp1.crc│       ├── part-timestamp2│       └── .part-timestamp2.crc</code></pre><p>Since the data stream will be replicated on disk, the performance will slow down due to file I/O. <strong>Local checkpoint</strong> privileges performance over fault-tolerance which will persist RDDs on local storage in the executors. Read or write will be faster in this case; however if a driver fails, the data not yet executed may not be recoverable. As default, the data storeage level is set to <code>MEMORY_AND_DISK</code> which saves data in cache and disk (some in cache and some in disk). For here I changed to <code>MEMORY_AND_DISK_SER_2</code> (more details can be referred to <a href="https://stackoverflow.com/questions/30520428/what-is-the-difference-between-memory-only-and-memory-and-disk-caching-level-in" target="_blank" rel="noopener">here</a>). The different is that, unlike <strong>cache</strong> only, the checkpoints doesn’t save DAG with all the parents of RDDs; instead, they only save particular RDDs and remain for a longer time than cache. The time of persistance is strictly related to the executed computation and ends with the end of the spark application. To apply the checkpoint machanism, you just simply need to set the checkpoint directory when you are creating the <strong>StreamingContext</strong></p><pre><code class="scala">def createContext(...params): StreamingContext = {    val sparkConf = new SparkConf().setAppName(&quot;KpiAnalysis&quot;)        val ssc = new StreamingContext(sparkConf, Seconds(1))     ...     ssc.checkpoint(CHECK_POINT_DIR)     ssc}</code></pre><p>and before an action is operated on RDD:</p><pre><code class="scala">    sRDD.checkpoint()    sRDD.foreachPartition { partitionOfRecords =&gt; {             ...        }    }</code></pre><p>and <code>sRDD.isCheckpointed()</code> will return <strong>true</strong>. For cleaning, the RDDs stored in cache will be cleaned with all other memory after the whole spark application is finished or terminated; the reliable RDDs stored on disk can be cleaned manually or set<br><code>spark.cleaner.referenceTracking.cleanCheckpoints</code> property to be <strong>true</strong> to enable automatic cleaning. This driver recovery mechanism is sufficient to ensure zero data loss if all data was reliably store in the system. However for my circumstance, the data is read from <strong>kafka</strong> and some of the data buffered in memory could be lost. If the driver process fails, all the executors running will be killed as well, along with any data in their memory. This pushes me to look for other mechanisms which are more advanced.<br><br></p><h2 id="Write-Ahead-Logs"><a href="#Write-Ahead-Logs" class="headerlink" title="Write Ahead Logs"></a>Write Ahead Logs</h2><p>Write Ahead Logs are used in database and file systems to ensure the durability of any data operations. The intention of the operation is first written down into a durable log , and then the operation is applied to the data. If the system fails in the middle of applying the operation, it can recover by reading the log and reapplying the operations it had intended to do.</p><p>Spark streaming uses <strong>Receiver</strong> to read data from <strong>Kafka</strong>. They run as long-running tasks in the executors and store the revecived data in the memory of the executors. If you enable the <strong>checkpoint</strong>, the data will be checkpointed either in cache or disk <strong>in executors</strong> before porceed into the application drivers. Unlike <strong>checkpoint</strong>, applying <strong>WAL</strong> will instead backup the recevied data in an <strong>external fault-tolerant filesystem</strong>. And after the executor batches the received data and sends to the driver, <strong>WAL</strong> supports another log to store the block metadata into external filesystem before being executed.<br><img src="/2019/05/22/Implement-Zero-Data-Loss-in-Spark-Streaming/wal_spark.JPG" alt="https://databricks.com/blog/2015/01/15/improved-driver-fault-tolerance-and-zero-data-loss-in-spark-streaming.html"><br>(diagram from <a href="https://databricks.com/blog/2015/01/15/improved-driver-fault-tolerance-and-zero-data-loss-in-spark-streaming.html" target="_blank" rel="noopener">https://databricks.com/blog/2015/01/15/improved-driver-fault-tolerance-and-zero-data-loss-in-spark-streaming.html</a>)<br>Spark streaming starts supporting WAL after version 1.2 and can be enabled by setting the config:</p><pre><code class="scala">val ssc = StreamingContext.createContext(...params)def createContext(...params): StreamingContext = {    val sparkConf = new SparkConf().setAppName(&quot;KpiAnalysis&quot;)                                   .set(&quot;spark.streaming.receiver.writeAheadLog.enable&quot;,&quot;true&quot;)        val ssc = new StreamingContext(sparkConf, Seconds(1))    ...    ssc.checkpoint(CHECK_POINT_DIR)    ssc}</code></pre><p>Set the spark automatically clean checkpoints to release disk memory:</p><pre><code class="scala">val ssc = StreamingContext.createContext(...params)def createContext(...params): StreamingContext = {    val sparkConf = new SparkConf().setAppName(&quot;KpiAnalysis&quot;)                                   .set(&quot;spark.streaming.receiver.writeAheadLog.enable&quot;,&quot;true&quot;)                                   .set(&quot;spark.cleaner.referenceTracking.cleanCheckpoints&quot;, &quot;true&quot;)        val ssc = new StreamingContext(sparkConf, Seconds(1))    ...    ssc.checkpoint(CHECK_POINT_DIR)    ssc}</code></pre><p>This will not clean the latest checkpoint as it is still referred to by the application to recover from possible failures. If you expect to restart the application driver if it crashed due to some errors and exactly start from where it crashed last time instead of performing the whole operation once again, you can create <strong>StreamingContext</strong> from the previous checkpoint by using the method <code>getOrCreate()</code>:</p><pre><code class="scala">val ssc = StreamingContext.getOrCreate(CHECK_POINT_DIR, () =&gt; createContext(...params))def createContext(...params): StreamingContext = {    val sparkConf = new SparkConf().setAppName(&quot;KpiAnalysis&quot;)                                   .set(&quot;spark.streaming.receiver.writeAheadLog.enable&quot;,&quot;true&quot;)                                   .set(&quot;spark.cleaner.referenceTracking.cleanCheckpoints&quot;, &quot;true&quot;)        val ssc = new StreamingContext(sparkConf, Seconds(1))    ...    ssc.checkpoint(CHECK_POINT_DIR)    ssc}</code></pre><p>However after I tried switching to <code>getOrCreate()</code>, I had the following exception:</p><pre><code>Exception in thread &quot;main&quot; org.apache.spark.SparkException: Failed to read checkpoint from directory checkpointDir    at org.apache.spark.streaming.CheckpointReader$.read(Checkpoint.scala:368)    at org.apache.spark.streaming.StreamingContext$.getOrCreate(StreamingContext.scala:827)    ...Caused by: java.io.IOException: java.lang.ClassCastException: cannot assign instance of com.some.project$$anonfun$1 to field org.apache.spark.streaming.dstream.MappedDStream.org$apache$spark$streaming$dstream$MappedDStream$$mapFunc of type scala.Function1 in instance of org.apache.spark.streaming.dstream.MappedDStream         at org.apache.spark.util.Utils$.tryOrIOException(Utils.scala:1310)    at org.apache.spark.streaming.DStreamGraph.readObject(DStreamGraph.scala:194)    at sun.reflect.NativeMethodAccessorImpl.invoke0(Native Method)    ...</code></pre><p>It is because this is not the first time I use checkpoints, and there already exsists some other checkpoints in the same root directory (etc, the simple checkpoint discussed previously). Since the application tried to read from those unrelated checkpoints and expected to cast them to generate new context, it would throw such <strong>ClassCastException</strong>. This can be easily solved by deleting original checkpoints in HDFS locals (manually).</p><p>WAL is an advanced checkpoint mechanism and also simple to be applied. Compared with checkpoint, it saves data into an external filesystem so that even though if the executor is terminated and the data in memory is clean-uped, data still can be recovered from the external filesystem. However. it can only recover the data which is logged in the filesystem, if the drivers fail due to some error, the executor will be terminated as well, so as the WAL writer. Then the rest incomming data will not be logged into the filesystem and hence, is not recoverable. It can be tested by calling <strong>stop</strong> to the <strong>StreamContext</strong>:</p><pre><code class="scala">def stop(stopSparkContext: Boolean, stopGracefully: Boolean): Unit</code></pre><p>Once it is called, the following console log interpretes that the <strong>WAL Writer</strong> is interrupted as well:</p><pre><code>ERROR ReceiverTracker: Deregistered receiver for stream 0: Stopped by driverWARN BlockGenerator: Cannot stop BlockGenerator as its not in the Active state [state = StoppedAll]     WARN BatchedWriteAheadLog: BatchedWriteAheadLog Writer queue interrupted.</code></pre><p>And also the data will not be recoverable across applications or Spark upgrades and hence not very reliable<br><br></p><h2 id="Kafka-Direct-API"><a href="#Kafka-Direct-API" class="headerlink" title="Kafka Direct API"></a>Kafka Direct API</h2><p>This mechanism is only available when you data source is <strong>Kafka</strong>. Kafka supports a commit strategy which is able to help you manage offsets of each topics. Each offset points to a slot in a topic. When the data stored in this slot is consumed by any receiver, Kafka will be acknowledged by this consumption and moves the index to the next data slot.<br><img src="/2019/05/22/Implement-Zero-Data-Loss-in-Spark-Streaming/Spark-Streaming-flow-for-offsets.JPG" alt="https://blog.cloudera.com/blog/2017/06/offset-management-for-apache-kafka-with-apache-spark-streaming/"><br>(diagram from <a href="https://blog.cloudera.com/blog/2017/06/offset-management-for-apache-kafka-with-apache-spark-streaming/" target="_blank" rel="noopener">https://blog.cloudera.com/blog/2017/06/offset-management-for-apache-kafka-with-apache-spark-streaming/</a>)<br>When <code>enable.auto.commit</code> is set to be true, as soon as any receiver retrieves the data from the offset datastore in Kafka (here I use Kafka to store offsets), the receiver will automatically commit, which doesn’t ensure that the data is successfully executed in the spark streaming. Therefore, we have to disbale the auto-commit when we are creating DStream from Kafka. After the data is successfully processed, we manually commit the offset to the datastore by calling the Kafka direct API:</p><pre><code class="scala">stream.foreachRDD { rdd =&gt;      //get current offset      val offsetRanges = rdd.asInstanceOf[HasOffsetRanges].offsetRanges      //process data      //store data in some datastore      DataStoreDB.push(...param)      //commit offset to kafka      stream.asInstanceOf[CanCommitOffsets].commitAsync(offsetRanges)    }  }</code></pre><p>If an error occurs at any point during the execution, the offset won;t be committed to the kafka offset store and hence the same data will be resent by kafka, which ensures that the data will be executed only once.</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;This is a log from my own experience in spark streaming during my work. Base on different environment and servers, the strategy may vary. For here, I am working with &lt;strong&gt;Kafka&lt;/strong&gt; and Google Cloud &lt;strong&gt;PubSub&lt;/strong&gt;.&lt;/p&gt;
    
    </summary>
    
    
      <category term="Kafka" scheme="https://ohyoooo.github.io/tags/Kafka/"/>
    
      <category term="Spark" scheme="https://ohyoooo.github.io/tags/Spark/"/>
    
  </entry>
  
  <entry>
    <title>如何用VS Code免翻墙听网易云音乐</title>
    <link href="https://ohyoooo.github.io/2019/05/22/%E5%A6%82%E4%BD%95%E7%94%A8VS-Code%E5%85%8D%E7%BF%BB%E5%A2%99%E5%90%AC%E7%BD%91%E6%98%93%E4%BA%91%E9%9F%B3%E4%B9%90/"/>
    <id>https://ohyoooo.github.io/2019/05/22/如何用VS-Code免翻墙听网易云音乐/</id>
    <published>2019-05-22T07:27:00.000Z</published>
    <updated>2019-07-31T08:54:49.514Z</updated>
    
    <content type="html"><![CDATA[<p>在墙外打开网易云音乐发现全是灰色的？ 抱歉您所在区域无法播放？ 该资源暂无版权？ 需要vip？<br>VS Code<strong>一行script</strong>全搞定～～ <a id="more"></a></p><ul><li><p>打开VS Code</p></li><li><p>左侧 Extensions 搜索 Netease Music (VSC Netease Music) 或者点<a href="https://marketplace.visualstudio.com/items?itemName=nondanee.vsc-netease-music" target="_blank" rel="noopener">这里</a></p></li><li><p>点击 Install 后重启 VS Code</p></li><li><p>作者提供了基于 VS Code 自身插件工具 Webview 实现的通过替换 electron 动态链接库翻墙的插件, 有详细的<a href="https://marketplace.visualstudio.com/items?itemName=nondanee.vsc-netease-music" target="_blank" rel="noopener">中文文档</a>, 这里 <strong>Unix Shell</strong> 用户（包括 <strong>MacOS</strong>）可以直接在 <strong>Terminal</strong> （任意dir）输入以下script:</p><pre><code>curl https://gist.githubusercontent.com/nondanee/f157bbbccecfe29e48d87273cd02e213/raw | python</code></pre><p>script 输出结果为:</p><pre><code>% Total    % Received % Xferd  Average Speed   Time    Time     Time  Current                               Dload  Upload   Total   Spent    Left  Speed100  3060  100  3060    0     0   8496      0 --:--:-- --:--:-- --:--:--  8476  vscode 1.34.0 x64electron 3.1.8download wellreplace doneremove temp</code></pre></li><li><p>替换完毕后打开 VS Code, 上方工具栏 Go -&gt; Go to file… 或者 <code>Command(⌘) P</code>, 输入：</p><pre><code>&gt;NeteaseMusic: Start</code></pre><p>等待 editor 跳出 <strong>Please preserve this webview tab</strong> 后就可以使用所有网易云的功能了, 注意这个tab页面必须要保留（不能关闭）</p></li><li><p>使用时直接<code>Command(⌘) P**</code>后输入命令即可, 命令都是以 <code>&gt;NeteaseMusic</code>起头, 输入 <code>&gt;ne</code> 后VS Code会自动跳出并补齐可行指令, 甚至还可以登陆收藏查看评论</p></li></ul>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;在墙外打开网易云音乐发现全是灰色的？ 抱歉您所在区域无法播放？ 该资源暂无版权？ 需要vip？&lt;br&gt;VS Code&lt;strong&gt;一行script&lt;/strong&gt;全搞定～～&lt;/p&gt;
    
    </summary>
    
    
      <category term="VS Code" scheme="https://ohyoooo.github.io/tags/VS-Code/"/>
    
      <category term="NeteaseMusic" scheme="https://ohyoooo.github.io/tags/NeteaseMusic/"/>
    
      <category term="网易云" scheme="https://ohyoooo.github.io/tags/%E7%BD%91%E6%98%93%E4%BA%91/"/>
    
  </entry>
  
  <entry>
    <title>Slice in Golang</title>
    <link href="https://ohyoooo.github.io/2019/05/12/Type-slice-in-Golang/"/>
    <id>https://ohyoooo.github.io/2019/05/12/Type-slice-in-Golang/</id>
    <published>2019-05-12T06:35:54.000Z</published>
    <updated>2019-07-31T08:54:49.514Z</updated>
    
    <content type="html"><![CDATA[<p>This article is a summary from <a href="https://blog.golang.org/go-slices-usage-and-internals" target="_blank" rel="noopener">Andrew Gerrand’s blog</a></p><p>Golang has an unique type <strong>slice</strong> which is an abstraction built on top of Go’s <strong>array</strong> type. They are really similar but providing different means of working with sequences of typed data. So to understand slices we must first understand arrays.<a id="more"></a><br><br></p><h2 id="Arrays-in-Go"><a href="#Arrays-in-Go" class="headerlink" title="Arrays in Go"></a>Arrays in Go</h2><p>An array in Go has to specify its <strong>length</strong> and <strong>element type</strong>. <strong>The size of the array is fixed and its length is part of its type</strong>. For example <code>[4]int</code> and <code>[5]int</code> are distinct and have different types even though they all store integers. And contrary to <strong>C/C++</strong>, the initial value of an array will be filled with <strong>0</strong> if it is not initialized.</p><pre><code class="Go">var a [4]inta[0] = 1i := a[0]j := a[1]//i == 1//j == 0</code></pre><p>Go’s arrays are values. <strong>An array variable denotes the entire array</strong>; it is not a pointer to the first array element (as would be the case in C/C++). This means that when you assign or pass around an array value you will make a copy of its contents. (To avoid the copy you could pass a pointer to the array, but then that’s a pointer to an array, not an array)</p><p>An array literal can be specified like so:</p><pre><code class="Go">b := [2]string{&quot;aa&quot;, &quot;bb&quot;}</code></pre><p>Or, you can have the compiler counting the array elements for you:</p><pre><code class="Go">b := [...]string{&quot;aa&quot;, &quot;bb&quot;}</code></pre><p>In both cases, the type of b is <strong>[2]string</strong>.<br><br></p><h2 id="Slices-in-Go"><a href="#Slices-in-Go" class="headerlink" title="Slices in Go"></a>Slices in Go</h2><p>Arrays are a bit inflexible, so you don’t see them too often in the code. Slices, though, are everywhere. Unlike an array type, a slice type has no specific length:</p><pre><code class="Go">b := []string{&quot;aa&quot;, &quot;bb&quot;}</code></pre><p>We can use build-in function <code>make()</code> to define a slice:</p><pre><code class="Go">func make([]T, len, cap) []T</code></pre><p><strong>T</strong> represent the type of the elements. Function <strong>make</strong> accepts type, length and capacity(optional) as parameters. When it is called, <strong>make</strong> will allocate an array and returns a slice that refers to that array</p><pre><code class="Go">var s []bytes = make([]byte, 5, 5)//s == []byte{0, 0, 0, 0, 0}</code></pre><p>If <strong>cap</strong> is not specified, it will be init as the value of <strong>len</strong>. We can use the build-in functions <code>len()</code> and <code>cap()</code> to check the length and capacity of a slice:</p><pre><code class="Go">len(s) == 5cap(s) == 5</code></pre><p>The zero value of a slice is <strong>nil</strong>. The len and cap functions will both return <strong>0</strong> for a nil slice.</p><p>A slice can also be formed by “slicing” an existing slice or array, for example, the expression b[1:4] creates a slice including elements 1 through 3 of b:</p><pre><code class="Go">b := []byte{&#39;a&#39;, &#39;b&#39;, &#39;c&#39;, &#39;d&#39;, &#39;e&#39;, &#39;f&#39;}// b[1:4] == []byte{&#39;b&#39;, &#39;c&#39;, &#39;d&#39;}, sharing the same storage as b</code></pre><p>The start and end indices of a slice expression are optional; they default to zero and the slice’s length respectively:</p><pre><code class="Go">// b[:2] == []byte{&#39;a&#39;, &#39;b&#39;}// b[2:] == []byte{&#39;c&#39;, &#39;d&#39;, &#39;e&#39;, &#39;f&#39;}// b[:] == b</code></pre><p>This is also the syntax to create a slice given an array:</p><pre><code class="Go">x := [3]string{&quot;Лайка&quot;, &quot;Белка&quot;, &quot;Стрелка&quot;}s := x[:] // a slice referencing the storage of x</code></pre><p>Slicing does not copy the slice’s data. It creates a new slice value that points to the original array. This makes slice operations as efficient as manipulating array indices. Therefore, modifying the elements of a re-slice modifies the elements of the original slice:</p><pre><code class="Go">d := []byte{&#39;a&#39;, &#39;b&#39;, &#39;c&#39;, &#39;d&#39;}e := d[2:]// e == []byte{&#39;c&#39;, &#39;d&#39;}// now change the re-slice will also change the original slice  e[1] = &#39;m&#39;// e == []byte{&#39;c&#39;, &#39;m&#39;}// d == []byte{&#39;a&#39;, &#39;b&#39;, &#39;c&#39;, &#39;m&#39;}</code></pre><p>A slice cannot be grown beyond its capacity. Attempting to do so will cause a <strong><em>runtime panic</em></strong>, just as when indexing outside the bounds of a slice or array. Similarly, slices cannot be re-sliced below zero to access earlier elements in the array.<br><br></p><h2 id="Double-the-capacity-of-a-slice"><a href="#Double-the-capacity-of-a-slice" class="headerlink" title="Double the capacity of a slice"></a>Double the capacity of a slice</h2><p>To increase the capacity of a slice, we must create a new, larger slice and <strong>copy</strong> the contents of the original slice into it. The belowing example shows how to create a new slice <strong>t</strong> whihc doubles the capacity of <strong>s</strong>:</p><pre><code class="Go">t := make([]byte, len(s), (cap(s) * 2))for i:= range s {    t[i] = s[i]}s = t   //reassign s to t</code></pre><p>The loop can be replaced by the build-in function <code>copy()</code>, which copies the data from source and returns the number of elements copied:</p><pre><code class="Go">func copy(dst, src []T) int</code></pre><p>The function <strong>copy</strong> supports copying between slices of different lengths (it will copy only up to the smaller number of elements) and the case that two slices refer to the same array. Using <strong>copy</strong>, the above double size code snippet can be rewritten as:</p><pre><code class="Go">t := make([]byte, len(s), (cap(s) * 2))copy(t, s)s = t</code></pre><p>A common operation is to append new data to the tail of a slice:</p><pre><code class="Go">func AppendByte(slice []byte, data ...type) []byte {    m := len(slice)    n := m + len(data)    if n &gt; cap(slice) { //if the original capacity is not big enough             newSlice := make([]byte, (n + 1) * 2)        copy(newSlice, slice)        slice = newSlice    }    slice = slice[0:n] //shrink the capacity to the length of data      copy(slice[m:n], data)    return slice}</code></pre><p>This customized AppendByte function is really useful because we can fully control the size of a slice. However most programs do need such complete control. Go provides a build-in function <code>append()</code> which appends slice x to the end of slice s, expanding s if needed:</p><pre><code class="Go">func append(s []T, x ...T) []T</code></pre><p>Using <strong>…</strong> to append one slice to the end of another:</p><pre><code class="Go">a := []string{&quot;aa&quot;, &quot;bb&quot;}b := []string{&quot;cc&quot;, &quot;dd&quot;}a = append(a, b...) //same as append(a, b[0], b[1], b[2])   </code></pre><p>Another example of append:</p><pre><code class="Go">func Filter(s []int, fn func(int) bool) []int {    var p []int // p == nil    for _, v := range s {        if fn(v) {            p = append(p, v)        }    }    return p}</code></pre>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;This article is a summary from &lt;a href=&quot;https://blog.golang.org/go-slices-usage-and-internals&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Andrew Gerrand’s blog&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;Golang has an unique type &lt;strong&gt;slice&lt;/strong&gt; which is an abstraction built on top of Go’s &lt;strong&gt;array&lt;/strong&gt; type. They are really similar but providing different means of working with sequences of typed data. So to understand slices we must first understand arrays.&lt;/p&gt;
    
    </summary>
    
    
      <category term="Golang" scheme="https://ohyoooo.github.io/tags/Golang/"/>
    
      <category term="Array" scheme="https://ohyoooo.github.io/tags/Array/"/>
    
      <category term="Slice" scheme="https://ohyoooo.github.io/tags/Slice/"/>
    
  </entry>
  
  <entry>
    <title>How to debug NodeJS on VS Code</title>
    <link href="https://ohyoooo.github.io/2019/05/08/How-to-debug-NodeJS-on-VS-Code/"/>
    <id>https://ohyoooo.github.io/2019/05/08/How-to-debug-NodeJS-on-VS-Code/</id>
    <published>2019-05-08T02:50:53.000Z</published>
    <updated>2019-07-31T08:54:49.505Z</updated>
    
    <content type="html"><![CDATA[<p>Here are the steps to start debug mode in VS Code:</p><ol><li><p>On the left side bar, click “debug” icon to switch to debug viewlet</p></li><li><p>On the top left, click the gear icon</p></li><li><p>Then <code>launch.json</code> will be opened in the editor</p></li><li><p>Replace the content of the file to be:</p><a id="more"></a><pre><code class="json">{ &quot;version&quot;: &quot;0.2.0&quot;, &quot;configurations&quot;: [     {         &quot;type&quot;: &quot;node&quot;,         &quot;request&quot;: &quot;launch&quot;,         &quot;name&quot;: &quot;Launch app.js&quot;,         &quot;program&quot;: &quot;${workspaceRoot}/app.js&quot;,         &quot;stopOnEntry&quot;: true,         &quot;args&quot;: [             &quot;arg1&quot;, &quot;arg2&quot;, &quot;arg3&quot;         ]     } ]}</code></pre></li><li><p>Replace the command line arguments to whatever you need</p></li><li><p>Start the debugger or press <code>F5</code></p></li></ol><p>You are all good to go!</p><p>If your program reads from <strong>stdin</strong>, please add a “console” attribute to the launch config:</p><pre><code class="json">{    &quot;version&quot;: &quot;0.2.0&quot;,    &quot;configurations&quot;: [        {            &quot;type&quot;: &quot;node&quot;,            &quot;request&quot;: &quot;launch&quot;,            &quot;name&quot;: &quot;Launch app.js&quot;,            &quot;program&quot;: &quot;${workspaceRoot}/app.js&quot;,            &quot;stopOnEntry&quot;: true,            &quot;args&quot;: [                &quot;arg1&quot;, &quot;arg2&quot;, &quot;arg3&quot;            ],            &quot;console&quot;: &quot;integratedTerminal&quot;        }    ]}</code></pre><p>If you are running the program in the <strong>terminal</strong>, you can change the content alternatively to be:</p><pre><code class="json">{    &quot;version&quot;: &quot;0.2.0&quot;,    &quot;configurations&quot;: [        {            &quot;type&quot;: &quot;node&quot;,            &quot;request&quot;: &quot;attach&quot;,            &quot;name&quot;: &quot;Attach to app.js&quot;,            &quot;port&quot;: &quot;5858&quot;        }    ]}</code></pre><p>The port is the <strong>debug port</strong> and it has nothing to do with your program (no matter it is a service or not). Then in the terminal, run the command:</p><pre><code class="shell">node --debug-brk app.js arg1 arg2 arg3...</code></pre><blockquote><p>The <code>--debug-brk</code> lets your program wait for the debugger to attach to. So there is no problem that it terminates before the debugger could attach.</p></blockquote><p><br></p><p>Running such command, you may encounter a warning below:</p><pre><code>(node:31245) [DEP0062] DeprecationWarning: `node --inspect --debug-brk` is deprecated. Please use `node --inspect-brk` instead.     </code></pre><p>As discussed in <a href="https://github.com/Microsoft/vscode/issues/32529" target="_blank" rel="noopener">microsoft github offical repository</a>, currently there is <strong>no way</strong> to prevent this happening. The reason why using <code>--inspect --debug-brk</code> is explained <a href="https://github.com/microsoft/vscode/issues/27731" target="_blank" rel="noopener">here</a>:</p><blockquote><p>This combination of args is the only way to enter debug mode across all node versions. At some point I’ll switch to inspect-brk if we don’t want to support node 6.x anymore, or will do version detection for it and do something for runtimeExecutable scenarios.</p></blockquote><blockquote><p>The problem is that we do not really know what version of node a user is using, so we cannot adapt the flags we use to the node version in order to minimize the resulting deprecation warnings.</p></blockquote>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;Here are the steps to start debug mode in VS Code:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;p&gt;On the left side bar, click “debug” icon to switch to debug viewlet&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p&gt;On the top left, click the gear icon&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p&gt;Then &lt;code&gt;launch.json&lt;/code&gt; will be opened in the editor&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p&gt;Replace the content of the file to be:&lt;/p&gt;&lt;/li&gt;&lt;/ol&gt;
    
    </summary>
    
    
      <category term="VS Code" scheme="https://ohyoooo.github.io/tags/VS-Code/"/>
    
      <category term="NodeJS" scheme="https://ohyoooo.github.io/tags/NodeJS/"/>
    
  </entry>
  
  <entry>
    <title>How to Check Open TCP/IP Ports in Mac OS X</title>
    <link href="https://ohyoooo.github.io/2019/05/06/How-to-Check-Open-TCP-IP-Ports-in-Mac-OS-X/"/>
    <id>https://ohyoooo.github.io/2019/05/06/How-to-Check-Open-TCP-IP-Ports-in-Mac-OS-X/</id>
    <published>2019-05-06T08:48:23.000Z</published>
    <updated>2019-07-31T08:54:49.505Z</updated>
    
    <content type="html"><![CDATA[<p>The core of Mac OS is Darwin and we can use most of the CLI tools in Mac OS just like how it feels like in Linux. If we want to check out the current ports in usage, the command <strong><code>netstat</code></strong> is useful:</p><pre><code>netstat -ap tcp | grep -i &quot;listen&quot;</code></pre><a id="more"></a><p>That will print out something like this in the console:</p><pre><code>Achive Internet connections(including servers)Proto     Recv-Q      Send-Q       Local Address       Foreign Address     (state)     tcp4      0           0            localhost.25035     *.*                  LISTEN</code></pre><p>That works but the problem is that it doesn’t show up the names of the procedures which occupy the ports. Sometimes we want to know precisely which program is exposing the port. </p><p>Then found out that there is another command <strong><code>lsof</code></strong>:</p><pre><code>sudo lsof -nP -iTCP:PortNumber -sTCP:LISTEN</code></pre><p>which prints out all the processes running in a given port with specific names:</p><pre><code>COMMAND    PID    USER    FD    TYPE    DEVICE    SIZE/OFF    NODE       NAMEsyslogd    350    root    5w    VREG    222,5     0           440818     /var/adm/messages     syslogd    350    root    6w    VREG    222,5     339098      6248       /var/log/syslogcron       353    root    cwd   VDIR    222,5     512         254550     /var -- atjobs</code></pre><p><strong><code>-n</code></strong> : No dns (no host name)<br><strong><code>-P</code></strong> : List port number instead of its name<br><strong><code>-i</code></strong> : Lists IP sockets</p><p>To view the port associated with a daemon:</p><pre><code>lsof -i -n -P | grep python</code></pre><p>If we just want to see the name:</p><pre><code>sudo lsof -i :PortNumber | grep LISTEN</code></pre><p>Get all running <strong>PID</strong> in a specific port:</p><pre><code>sudo lsof -i :PortNumber| grep LISTEN | awk &#39;{ print $2; }&#39; | head -n 2 | grep -v PID   </code></pre><p>And then we can kill all the processes:</p><pre><code>sudo kill -9 $(sudo lsof -i :PortNumber| grep LISTEN | awk &#39;{ print $2; }&#39; | head -n 2 | grep -v PID)   </code></pre><p>list all commands:</p><pre><code>lsof -h</code></pre>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;The core of Mac OS is Darwin and we can use most of the CLI tools in Mac OS just like how it feels like in Linux. If we want to check out the current ports in usage, the command &lt;strong&gt;&lt;code&gt;netstat&lt;/code&gt;&lt;/strong&gt; is useful:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;netstat -ap tcp | grep -i &amp;quot;listen&amp;quot;
&lt;/code&gt;&lt;/pre&gt;
    
    </summary>
    
    
      <category term="CLI" scheme="https://ohyoooo.github.io/tags/CLI/"/>
    
      <category term="Mac OS" scheme="https://ohyoooo.github.io/tags/Mac-OS/"/>
    
      <category term="port" scheme="https://ohyoooo.github.io/tags/port/"/>
    
  </entry>
  
  <entry>
    <title>Memory Leaks in Serveral Commonly Used Programming Languages</title>
    <link href="https://ohyoooo.github.io/2019/05/05/Memory-Leak-in-Serveral-Commonly-Used-Programming-Languages/"/>
    <id>https://ohyoooo.github.io/2019/05/05/Memory-Leak-in-Serveral-Commonly-Used-Programming-Languages/</id>
    <published>2019-05-05T06:37:22.000Z</published>
    <updated>2019-08-03T21:35:01.408Z</updated>
    
    <content type="html"><![CDATA[<p>Usually when we talk about memory leaks we are actually talking about the memory leaks in heap memory. When an object is initialized, it will be dynamically allocated to a piece of memory in the heap and ready to be manipulated. After we perform some operations and the whole procedure is finished, the object stored in heap should also be erased; however in the case of memory leak, that piece of memory is not released but still held in the heap, marked as occupied but no reference refers to it.<a id="more"></a></p><p>Wiki’s Def:</p><blockquote><p><a href="https://en.wikipedia.org/wiki/Memory_leak" target="_blank" rel="noopener"><strong>Memory leak</strong></a> is a type of resource leak that occurs when a computer program incorrectly manages memory allocations in such a way that: </p><ul><li>memory which is no longer needed is not released</li><li>an object is stored in memory but cannot be accessed by the running code</li></ul></blockquote><p>We usually encounter this issue in programming languages that don’t have <a href="https://en.wikipedia.org/wiki/Garbage_collection_(computer_science" target="_blank" rel="noopener"><strong>GC</strong></a>, for example C++ and C. For such languages, we have to manage the memory by ourselves which, if not done properly, will expose the risks of memory leaks.<br><br></p><h2 id="This-is-really-common-in-C"><a href="#This-is-really-common-in-C" class="headerlink" title="This is really common in C++"></a>This is really common in C++</h2><p>Let’s take a look in C++. There are literally hundreds of ways that can cause memory leaks and most of them won’t be detected during compilation and even in runtime. Only a few leaks will not have any impact on the system; however if we are running a huge application and those leaks accumulate, that will significantly reduce the real runtime performance of the whole system.</p><p>We all know that when we allocate an object, we have to release the memory if this object is not used anymore. The way we release the memory is simply call the buid-in function <strong><code>free()</code></strong> or <strong><code>delete[]</code></strong>. However in C++ the procedure can exit anywhere. An exception can be thrown in the half way so that the code doesn’t ever reach the line to release memory:</p><pre><code class="c++">int sample(int n) {    void  *ptr = malloc(16);    if (n)        return -1; //memory leak here    free(ptr);    return 0;}</code></pre><p>or:</p><pre><code class="c++">class Sample {    public:        init() { int *n = new int;  throw any_exception(); }        ~init() { delete n; }    private:        int *n;};Sample *n = new Sample; //memory leak here</code></pre><p>The solution to the above examples is also really simple: check control flows and <strong>do remember to call the destructor before anywhere the procedure may exit</strong>. Well if you want to do it in a fancy way, you can use <strong><em>smart pointer</em></strong> alternatively:</p><pre><code class="c++">class Sample {    public:        init() { n = std::make_shared&lt;int&gt;(new int) }        ~init() {}    private:        std::shared_ptr&lt;int&gt; n;};</code></pre><p>Smart pointer helps you manage this object and if it is not referred anymore, release its memory.<br><br></p><h2 id="free-delete-is-not-enough"><a href="#free-delete-is-not-enough" class="headerlink" title="free( )/delete is not enough"></a>free( )/delete is not enough</h2><p>Now your program has such a concrete control flow that <strong>free( )</strong> or <strong>delete</strong> is called before all the possible drop out. That is great but still not enough. <strong>free( )</strong> and <strong>delete</strong> can <strong>only release the memory where the pointer is currently pointing to but not the pointer itself!</strong> The pointer will still point to the original memory address but the content has been already removed. In this circumstance, the value of the pointer does not equal to <strong>NULL</strong>, instead some random values that cannot be predicted.</p><pre><code class="c++">int main() {    char *p = (char*) malloc(sizeof(char) * 100);    strcpy(p, &quot;hello&quot;);    free(p);    if (p != NULL) //doesn&#39;t prevent issue        strcpy(p, &quot;world&quot;); // error}</code></pre><p>This pointer p is called <a href="https://en.wikipedia.org/wiki/Dangling_pointer" target="_blank" rel="noopener"><strong><em>dangling pointer</em></strong></a> or <a href="https://en.wikipedia.org/wiki/Dangling_pointer" target="_blank" rel="noopener"><strong><em>wild pointer</em></strong></a> and will only be erased after the whole procedure is finished or terminated. The wild pointer is really risky because of its random behavior. Imagine there is something in your room that sometimes can be observed sometimes cannot, randomly breaks your stuff but never leaves footprint. In programming it is called wild pointer, and in real life it is called <a href="https://en.wikipedia.org/wiki/Cat" target="_blank" rel="noopener"><strong>cat</strong></a>. To prevent it, we should <strong>always set the pointer to be NULL when it is not used/the memory is released</strong>.</p><p><strong><em>Note</em></strong>: when you define a pointer without setting up its initial value, that pointer will also be a <strong>wild pointer</strong> and has a value of some random number (which doesn’t equal to <strong>NULL</strong>). Hence it is necessary to set the value of a pointer to be <strong>NULL</strong> if it cannot be asigned a value at the beginning.</p><p>For some simple pointers, they can be reasigned to <strong>NULL</strong> to prevent <strong>wild pointer</strong>, however for a pointer referring to a hierarchical object, simply setting to <strong>NULL</strong> cannot resolve the potential issues. For example, you are using a <strong><code>vector</code></strong> in C++ :</p><pre><code class="c++">vector &lt;string&gt; vint main() {    for (int i=0; i&lt;1000000; i++)        v.push_back(&quot;test&quot;);    cout &lt;&lt; v.capacity() &lt;&lt; endl;  //memory usage: 54M    v.clear();    cout &lt;&lt; v.capacity() &lt;&lt; endl;  //memory usage: still 54M}</code></pre><p>Even though we have cleared the vector and all its elements were indeed released, the capacity of the vector is still unchanged. <strong><code>clear()</code></strong> removed all its element but cannot shrink the size of the container. The same thing happens to other containers such as <strong><code>deque</code></strong>. To handle this, before <strong>C++ 11</strong>, we can swap the pointers:</p><pre><code class="c++">int main() {    ...    v.clear();    vector&lt;string&gt;(v).swap(v); //new a vector with the same content and swap        cout &lt;&lt; v.capacity() &lt;&lt; endl;  //memory usage: 0}</code></pre><p>after C++ 11, it provides function <strong><code>shrink_to_fit()</code></strong> to remove the extra allocated memory.<br><br></p><h2 id="GC-doesn’t-avoid-memory-leaks"><a href="#GC-doesn’t-avoid-memory-leaks" class="headerlink" title="GC doesn’t avoid memory leaks"></a>GC doesn’t avoid memory leaks</h2><p>It’s not suprising that GC can prevent most cases of memory leaks because it is runnig in an individual thread, checking the memory regularly and removing the unreferred objects. It is so powerful that porgrammers rarely pay attention to memory management and be aware of the memory leaks. <strong>Java</strong> is such language which has powerful and unruly GC that can be hardly controlled (call <strong><code>System.gc()</code></strong> doesn’t certainly invoke GC). It helps to manage the memory in jvm, but it cannot avoid memory leaks.</p><p>There are mainly two cases that can lead to memory leaks in Java. One is the object which has a longer lifecycle keeps a reference to another object which has a shorter lifecycle:</p><pre><code class="java">public class Sample {    Object object;    public void anymethod(){        object = new Object();        ...    }    ...}</code></pre><p>If <strong><em>object</em></strong> is only used inside <strong><em>anymethod( )</em></strong>, then after stack pops <strong><em>anymethod( )</em></strong>, the lifecycle of <strong><em>object</em></strong> should also be ended. But for here, because class <strong><em>Sample</em></strong> is still proceeding and keeps the reference of <strong><em>object</em></strong>, <strong><em>object</em></strong> cannot be collected by GC and hence leaks the memory. The solution will be either init <strong><em>object</em></strong> inside <strong><em>anymethod( )</em></strong> (as a local varible) or set <strong><em>object</em></strong> to be <strong><em>null</em></strong> after <strong><em>anymethod</em></strong> is finished.</p><p>Another case is the use of <strong><code>HashSet</code></strong>. <strong><em>HashSet</em></strong> is the implementation of hash-table and it stores elements according to their different hash values. In order to push and withdraw the same object in the <strong><em>HashSet</em></strong>, we need to override the method <strong><code>HashCode()</code></strong> so that the same object has the same hash vaule and being stored in the same place in <strong><em>HashSet</em></strong>. However, if we push something into the <strong><em>HashSet</em></strong> and then change some properties of this object (those properties are most likely to be used to calculate the hashcode), the hashcode of this object may vary and when we refer this object back to our <strong><em>HashSet</em></strong> to do some operations, for example delete this object from the <strong><em>HashSet</em></strong>, this object might not be found in the set and hence cannot be deleted:</p><pre><code class="java">    HashSet&lt;Obejct&gt; set = new HashSet&lt;Object&gt;();    Object something = new Object();    set.add(something);    something.doSomethingChanges();    set.contains(something);  //this may return false    set.remove(something);  //&#39;something&#39; cannot be removed if the previous line returns false      </code></pre><p><br></p><h2 id="Python"><a href="#Python" class="headerlink" title="Python"></a>Python</h2>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;Usually when we talk about memory leaks we are actually talking about the memory leaks in heap memory. When an object is initialized, it will be dynamically allocated to a piece of memory in the heap and ready to be manipulated. After we perform some operations and the whole procedure is finished, the object stored in heap should also be erased; however in the case of memory leak, that piece of memory is not released but still held in the heap, marked as occupied but no reference refers to it.&lt;/p&gt;
    
    </summary>
    
    
      <category term="NodeJS" scheme="https://ohyoooo.github.io/tags/NodeJS/"/>
    
      <category term="C++" scheme="https://ohyoooo.github.io/tags/C/"/>
    
      <category term="Java" scheme="https://ohyoooo.github.io/tags/Java/"/>
    
      <category term="Python" scheme="https://ohyoooo.github.io/tags/Python/"/>
    
  </entry>
  
  <entry>
    <title>Prefix Notation</title>
    <link href="https://ohyoooo.github.io/2019/04/30/Prefix-Notation/"/>
    <id>https://ohyoooo.github.io/2019/04/30/Prefix-Notation/</id>
    <published>2019-04-30T02:11:21.000Z</published>
    <updated>2019-07-31T08:54:49.507Z</updated>
    
    <content type="html"><![CDATA[<pre><code class="python">( 20 + 5 )( 16 / 4 )</code></pre><p>Such expressions which denote procedures, are called <strong><em>combinations</em></strong>. The left and the right elements are called <strong><em>operands</em></strong>, and the element in the middle to indicate the operation is called <strong><em>operator</em></strong>. This is the most common style we have seen by now; however there is another way to construct a procedure known as <strong><em>prefix notation</em></strong>:</p><pre><code class="python">( + 20 5 )( / 16 4 )</code></pre><p>Instead of injecting the operator between operands, which is a more human readable style, the prefix notation requires the operator always to be at the left most.<a id="more"></a></p><p>conditions:</p><pre><code class="python">( define ( abs x )    ( cond (( &gt; x 0 ) x )           (( = x 0 ) 0 )           (( &lt; x 0 ) ( - x ))))</code></pre><p>The general form can be expressed as:</p><blockquote><p>( cond (&lt;\P1&gt; &lt;\E1&gt;)<br>      (&lt;\P2&gt; &lt;\E2&gt;)<br>           …<br>      (&lt;\Pn&gt; &lt;\En&gt;))</p></blockquote><p>If none of them is evaluated to be <strong>true</strong>, then the value of the <strong>cond</strong> will be <strong>undefined</strong>. It can also be simplified by using <strong><em>else</em></strong>:</p><pre><code class="python">( define ( abs x )    ( cond (( &lt; x 0 ) ( - x ))           ( else  x )))</code></pre><p>If there is only two <strong><em>predicates</em></strong> (the expression to be interpreted as either true of false), then it can use a special form <strong><em>if</em></strong>:</p><pre><code class="python">( define ( abs x )    ( if ( &lt; x 0 )         ( - x )         x ))</code></pre><p>The general form of an <strong><em>if</em></strong> expression is:</p><blockquote><p>( if &lt;\predicate&gt; &lt;\consequent&gt; &lt;\alternative&gt; )</p></blockquote><p>The logic operators:</p><blockquote><p>( and &lt;\E1&gt; … &lt;\En&gt; )<br>( or &lt;\E1&gt; … &lt;\En&gt; )<br>( not <e> )</e></p></blockquote><p>Then use the logic operators to define a predicate to evaluate if a number id larger or equal to the other one:</p><pre><code class="python">( define ( &gt;= x y )    ( or ( &gt; x y ) ( = x y ))</code></pre><p>That is all the syntax, <strong>there is no loop in a functional programming language!</strong><br><br></p><h2 id="Recursion"><a href="#Recursion" class="headerlink" title="Recursion"></a>Recursion</h2><p>Considering the factorial function:</p><blockquote><p>n! = n ⋅ (n-1) ⋅ (n-2) ⋅ … ⋅2⋅1</p></blockquote><p>Which can be computed as:</p><blockquote><p>n! = n ⋅ (n-1)!</p></blockquote><p>If we end it up with <strong>1!</strong>, then simply output <strong>1</strong>. Then the factorial function can be implemented in <strong><em>linear recursion</em></strong>:</p><pre><code class="python">( define ( factorial n )    ( if ( = n 1 )        1        ( * n ( factorial ( - n 1 )))))</code></pre><p><strong><em>Linear recursion</em></strong> defines that the computation chains of operations is proportional to n and hence grows linearly. There is also another pattern of recursion, known as <strong><em>Tree Recursion</em></strong>. The best example will be the Fibonacci series, in which each element is the sum of the previous two:</p><pre><code class="python">( define ( fib n )    ( cond ( = n 0 ) 0 )           ( = n 1 ) 1 )           ( else ( + ( fib ( - n 1 ) )                      ( fib ( - n 2 ) )))))</code></pre><p>You may find out that this procedure is not really efficient because to compute <strong>fib( - n 1)</strong>, <strong>fib( - n 2)</strong> has to be computed one more time which causes duplicated work.<br><img src="../images/treeRecursion.png" alt="Tree Recursion"><br>Therefore, instead of <strong><em>Tree Recursion</em></strong>, let’s try to convert it to be <strong><em>Linear Recursion</em></strong>. Reasign the sum of <strong>a</strong> and <strong>b</strong> to <strong>a</strong>, and the previous <strong>a</strong> to <strong>b</strong>:</p><pre><code class="python">( define ( fib n )    ( iterate 1 0 n ))( define ( iterate a b count )    ( if ( = count 0 )        b        ( iterate ( + a b ) a ( - count 1 ))))</code></pre><p><br></p><h2 id="Lambda"><a href="#Lambda" class="headerlink" title="Lambda"></a>Lambda</h2><p>Instead of defining some trivial procedures so that we can pass them as arguments of the other procedures, functional programming provides <strong><em>Lambda Expression</em></strong>:</p><blockquote><p>( lambda ( &lt;\formal-param&gt; ) &lt;\body&gt; )</p></blockquote><p>For instance,</p><pre><code class="python">( define ( Add a b ) ( + a b ))</code></pre><p>can be written as:</p><pre><code class="python">( define add ( lambda ( a b ) ( + a b )))</code></pre><p>And operators can also be represented by <strong><em>Lambda Expression</em></strong>:</p><pre><code class="python">(( lambda ( a b ) ( + ( * a a ) ( * b b ))) 2 3 )</code></pre><p>Another use of <strong><em>Lambda Expression</em></strong> is creating local variables. An expression can be binded with a specific name by using keyword <strong><em>let</em></strong>. The above example then can be interpreted as:</p><pre><code class="python">( define ( sumsqr x y )    ( let ( a ( * x x ))          ( b ( * y y ))        ( + a b )))</code></pre><p><strong><em>Note:</em></strong> The scope of a variable specified by a <strong><em>let</em></strong> is only applied to the <strong>body</strong> of the <strong><em>let</em></strong>. For example, if the evalue of <strong>x</strong> is <strong>2</strong>, then the expression:</p><pre><code class="python">( let (( x 3 )        ( y ( + x 2 )))    ( * x y ))</code></pre><p>The value of <strong>y</strong> will be <strong>4</strong> as being outside of the <strong>let</strong> body, and the output will be <strong>3 * 4 = 12</strong>. It seems like <strong><em>let</em></strong> is really similar to <strong><em>define</em></strong>; however, in the most cases, we much prefer using <strong><em>let</em></strong> and only apply <strong><em>define</em></strong> to <strong>internal procedures</strong>.</p>]]></content>
    
    <summary type="html">
    
      &lt;pre&gt;&lt;code class=&quot;python&quot;&gt;( 20 + 5 )
( 16 / 4 )
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Such expressions which denote procedures, are called &lt;strong&gt;&lt;em&gt;combinations&lt;/em&gt;&lt;/strong&gt;. The left and the right elements are called &lt;strong&gt;&lt;em&gt;operands&lt;/em&gt;&lt;/strong&gt;, and the element in the middle to indicate the operation is called &lt;strong&gt;&lt;em&gt;operator&lt;/em&gt;&lt;/strong&gt;. This is the most common style we have seen by now; however there is another way to construct a procedure known as &lt;strong&gt;&lt;em&gt;prefix notation&lt;/em&gt;&lt;/strong&gt;:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;python&quot;&gt;( + 20 5 )
( / 16 4 )
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Instead of injecting the operator between operands, which is a more human readable style, the prefix notation requires the operator always to be at the left most.&lt;/p&gt;
    
    </summary>
    
    
      <category term="Lisp" scheme="https://ohyoooo.github.io/tags/Lisp/"/>
    
      <category term="Scheme" scheme="https://ohyoooo.github.io/tags/Scheme/"/>
    
      <category term="Prefix Notation" scheme="https://ohyoooo.github.io/tags/Prefix-Notation/"/>
    
      <category term="Functional Programming" scheme="https://ohyoooo.github.io/tags/Functional-Programming/"/>
    
  </entry>
  
  <entry>
    <title>A Brief Introduce to Redux Saga</title>
    <link href="https://ohyoooo.github.io/2019/04/28/A-Brief-Introduce-to-Redux-Saga/"/>
    <id>https://ohyoooo.github.io/2019/04/28/A-Brief-Introduce-to-Redux-Saga/</id>
    <published>2019-04-28T05:35:50.000Z</published>
    <updated>2019-07-31T08:54:49.505Z</updated>
    
    <content type="html"><![CDATA[<p>If you are quite experienced with redux, which is a predictable state container for JavaScript applications (<strong>Note:</strong> even thouth React and Redux is a popular combination to build fast and powerful apps, Redux is not necessarily combined with React), you are definitely feeling comfortable with its powerful store which manages all the global states and provides much cleaner logic flows to change them. If you are new to redux, <a href="https://redux.js.org/introduction/getting-started" target="_blank" rel="noopener">here</a> is the guide to dive before we start our topic.<a id="more"></a></p><p>In a complex javascript application, asynchronous function is always one of the most annoying part where encounters tons of bugs. If not handle them properly, the app usually ends up with <strong><em>call back hell</em></strong>.<br><br></p><h2 id="Haven’t-heard-of-CallBack-Hell"><a href="#Haven’t-heard-of-CallBack-Hell" class="headerlink" title="Haven’t heard of CallBack Hell?"></a><strong>Haven’t heard of <em>CallBack Hell</em>?</strong></h2><p>Well, in javascript, the only way you can suspend a computation and have the rest operations doing later is to put the rest operations into a callback function. This callback function usually returns a <strong><code>Promise</code></strong> (And has a type of <strong><code>Promise&lt;any&gt;</code></strong>). In order to easily mark those async functions, after <strong><em>ES6</em></strong> javascript provides extra modifiers <strong><code>async</code></strong> and <strong><code>await</code></strong>, which actually wraps up the original utilities of promise and makes it more readable to programmers. Hummm, sounds like things are going better… <del><strong>NO!! It doesn’t resolve anything!</strong></del> The core problem leads to a callback hell is the hierarchical async calls, for example</p><p>you have some simple synchronous functions which are in a chain to accomplish some logics:</p><pre><code class="javascript">a = getSomething();b = getMore(a);c = getMoreAndMore(b);...</code></pre><p>It looks fine for now, but what if they all turn out to be async? Then you have to follow the callback style to make them operate one right after another is done:</p><pre><code class="javascript">getSomthing(function(a) {    getMore(a, function(b) {        getMoreAndMore(b, function(c) {            //keep going...        });    });});</code></pre><p>Or you prefer <strong><em>ES6</em></strong>:</p><pre><code class="javascript">async function getSomething(a) {    await b = ToDo(a);    return await getMore((b) =&gt; {        return await ToDo(b);    }).then((c) =&gt; {        return await ToDo(c);    }).then(...);}</code></pre><p>Looks really confused? This will getting even uglier if we are using callbacks in loops.<br><br></p><h2 id="Redux-Thunks"><a href="#Redux-Thunks" class="headerlink" title="Redux Thunks"></a>Redux Thunks</h2><p>Back to our redux app, we usually want to update some states after an async call to inform the UI that the data is ready to be fetched. That is always achieved by dispatching an action from the component to the reducer:</p><pre><code class="javascript">async const callAPI = () =&gt; {    ...    return response;};...async const updateUI = (...params) =&gt; {    const res = await callAPI();    if (res.status === 200)        dispatch({type: &quot;UPDATE&quot;, isSuccess: true});};...render({    ...    this.props.isSuccess?        showData() : showError()});</code></pre><p>This isn’t bad, but we are always looking for something better. An advanced way to rewrite it is using redux middleware. <strong><em>Middleware</em></strong> is somewhere you can put the code after the framework receives a request and before it generates a response. For example, we want to add a logger in the redux store so that when the store catches an action, before it returns the new state, the logger can log the previous state and the new generated state. This is what can be added as a middleware:</p><pre><code class="javascript">function logger(store) {    return function wrapDispatch (next) {        return function dispatchAndLog (action) {            console.log(&quot;dispatching.. &quot;, action);            let result = next(action);            console.log(&quot;new state&quot;, store.getState());            return result;        }    }}</code></pre><p>There are more advanced ways to add a logger. If you are interested, please refer to the <a href="https://redux.js.org/advanced/middleware" target="_blank" rel="noopener">offical documentation</a>. With our middleware, the previous example can be written in a cleaner way:</p><pre><code class="javascript">const callAPI = () =&gt; {    return((dispatch) =&gt; {        dispatch(startCallingApiAction);        actualCallApi().then(data =&gt; {            dispatch(successAction(data));        }).fail(err =&gt; {            dispatch(failedAction(err));        });    });};</code></pre><p>The successful response data is wrapped in the payload of the action, sent to the reducer. Once the store updates the data, it will be mapped as a prop back to the component and request for a rerender. This middleware is also called <strong><em>thunk</em></strong>. By applying thunk to decouple the presentation layer, we can get rid of most of the side effects in components, instead, managing and orchestrating side effects in thunks.</p><p>This is great, so why are we even considering <strong><em>saga</em></strong>? Well, one of the advantages of middleware is that it can be chained. Every middleware mounted in redux store starts an individual thread (or something really looks like a thread in <strong><em>NodeJS</em></strong>). When a middleware captures an action and handles its side effect, it can dispatch a new action to another middleware to do nested logics. This behavior of middleware indicates that thunks can be chained as well, for example thunkA forwards its return payload to thunkB and thunkB forwards its return payload to… <strong>Wait! That sounds quite familiar!! Is that the case of callback hell??</strong> Unfortunately, a good thing plus another good feature doesn’t always end up with something better. <del>It could be some shit as well (笑)</del> In this case, true, this is exactly the callback hell.<br><br></p><h2 id="Redux-Saga"><a href="#Redux-Saga" class="headerlink" title="Redux Saga"></a>Redux Saga</h2><p>To handle the possible endless callback functions and also to make it more easily to test in a component which has complicated logics, we need to change our previous thoughts. Just like shifting from Process Oriented Programming to Object Oriented Programming, instead of telling the application how to handle the side effects, suppose it already knows how to call a function and how to dispatch an action, all we need to do is to <strong>give instructions about what to do next</strong> and we don’t care about how those instructions will be executed (Saga handles the executions).</p><p>Then the thunks example can be changed as following:</p><pre><code class="javascript">export function* apiSideEffect(action) {    try{        const data = yield call(actualCallApi);        yield put({ type:&quot;SUCCESS&quot;, payload: data });    } catch(err) {        yield put({ type:&quot;FAILED&quot;, payload: err });    }}export function* apiSaga() {    yield takeEvery(&quot;CLICK_TO_CALL_API&quot;, apiSideEffect);}</code></pre><p>There are serval fucntions already being integrated in Saga:</p><blockquote><p><strong><code>Call</code></strong>: the method call will return only a plain object describing the operation so redux-saga can take care of the invocation and returns the result to the generator. The first parameter is the generator function ready to be called and the rest params are all the arguments in the generator.</p></blockquote><blockquote><p><strong><code>Put</code></strong>: Instead of dispatching an action inside the generator (Don’t ever ever do that), <strong><em>put</em></strong> Returns an object with instructions for the middleware to dispatch the action.</p></blockquote><blockquote><p><strong><code>Select</code></strong>: Returns value from the selector function, similar with <strong><code>getState()</code></strong>. <strong><em>Note:</em></strong> It is not recommended to use this function because it returns the value corresponding to the contents of the store state tree, which is most likely a plain Javascript object and is <strong>mutable</strong> (Redux wants you to handle state immutably, which means return a new state instead of changing the old one).</p></blockquote><blockquote><p><strong><code>Take</code></strong>: It creates a command object that tells the middleware to wait for a specific action. The resulting behavior of the call Effect is the same as when the middleware suspends the generator until a <strong><em>promise</em></strong> resolves. In the take case, it’ll suspend the generator until a matching action is dispatched</p></blockquote><p>By working with Saga, we make the side effects to be <strong><em>declarative</em></strong> rather than <strong><em>imperative</em></strong>.</p><blockquote><p><strong><em>Declarative:</em></strong> describing what the program must accomplish, rather than describe how to accomplish it</p></blockquote><blockquote><p><strong><em>Imperative:</em></strong> consists of commands for the computer to perform, focuses on describing how a program operates</p></blockquote><p>In the case of take, the control is inverted. Instead of the actions being pushed to the handler tasks, the <strong>Saga is pulling the action by itself</strong>. An additional generator, known as <strong><em>watcher</em></strong> which contains <strong><em>take</em></strong> has to be created to watch a specific action and being triggered once the following action is dispatched in the application. There are two ways to create a watcher, one is using the buid-in functions (<strong><em>Saga Helper</em></strong>):</p><pre><code class="javascript">function* watchFetchData() {    yield takeEvery(&quot;FETCH_REQUEST&quot;, callFetchDataApi);}</code></pre><p><strong><em>takeEvery</em></strong> allows multiple request to be proceeding at the same time. Or if you just want the latest request to be fired (the older one will be overrided during each time the watcher is triggered</p><pre><code class="javascript">function* watchFetchData() {    yield takeLatest(&quot;FETCH_REQUEST&quot;, callFetchDataApi);}</code></pre><p>However by using <strong><em>take</em></strong>, it is possible to fully control an action observation process to build complex control flow:</p><pre><code class="javascript">function* watchFetchData() {    while(true) {        const action = yield take(&quot;FETCH_REQUEST&quot;);        console.log(action);        yield call(callFetchDataApi, action.payload);    }}</code></pre><p>All right, now you have been exposed to everything you need to know before start trying redux saga on your own. Here is a short overall example that may also help:<br>Store:</p><pre><code class="javascript">const sagaMiddleware = createSagaMiddleware();const store = createStore(rootReducer, appluMiddleware(sagaMiddleware));    sagaMiddleware.run(watchFetch);</code></pre><p>Sagas:</p><pre><code class="javascript">function* watchFetch(): Generator&lt;*, *, *&gt; {    yield takeEvery(&quot;FETCH_ACTION&quot;, callFetchAPI);}function* callFetchAPI(): Generator&lt;*, *, *&gt; {    try {        yield put({ type: &quot;FETCHING&quot;, payload: ... });        const data = yield call(actualCallApi);        yield put({ type: &quot;FETCH_SUCCESS&quot;, payload: data });    } catch(err) {        yield put({ type: &quot;FETCH_FAILED&quot;, payload: err });    }}</code></pre><p>Reducer:</p><pre><code class="javascript">const reducer = (state = initState, action) =&gt; {    switch(action) {        case &quot;FETCHING&quot;:            return { loading: true, ...state };        case &quot;FETCH_SUCCESS&quot;:            return { loading: false, success: true, data: action.payload, ...state };           case &quot;FETCH_FAILED&quot;:            return { loading: false, success: false, error: true, ...state };        default:            return { ...state };    }};</code></pre><p>Component:</p><pre><code class="javascript">class myComponent extends React.Component {    const mapStateToProps = ...    const mapDispatchToProps = ...    render() {        return (            &lt;button onClick = { () =&gt; this.props.dispatch({type: &quot;FETCH_ACTION&quot;}) }/&gt;               {                this.props.loading?                    &lt;p&gt;Loading..&lt;/p&gt; : this.props.error?                        &lt;p&gt;Error!&lt;/p&gt; : &lt;p&gt;{this.props.data}&lt;/p&gt;            }        );    }}export default connect(mapStateToProps, mapDispatchToProps)(myComponent);</code></pre><p>For more advanced concepts, there is a well-organized <a href="https://redux-saga.js.org/docs/advanced/" target="_blank" rel="noopener">Saga offical documentation</a> you can refer to if you want to dive deeper.<br><br></p><h2 id="How-to-test-Saga"><a href="#How-to-test-Saga" class="headerlink" title="How to test Saga?"></a>How to test Saga?</h2><p>A function that returns a simple object is easier to test than a function that directly makes an asynchronous call. For redux saga, each time you yield a function call will return a plain javascript object which makes the workflow much easier to test. You don’t need to use the real API, fake it, or mock it, instead just iterating over the generator function, asserting for equality on the values yielded.</p><pre><code class="javascript">describe(&quot;fetch work flow&quot;, () =&gt; {    const generator = cloneableGenerator(callFetchAPI)({ type: &quot;FETCH_ACTION&quot; });    expect(generator.next().value).toEqual(put({ type: &quot;FETCHING&quot;, payload: ... }));        test(&quot;fetch success&quot;, () =&gt; {        const clone = generator.clone();        expect(clone.next().value).toEqual(put({ type: &quot;FETCH_SUCCESS&quot; }));        expect(generator.next().done).toEqual(true);    });});</code></pre><p>In the above example, we use <strong><code>clone()</code></strong> to test different control flows and <strong><code>next()</code></strong> to iterate to the next function ready be yielded. The mock return value can also be injected as an argument of <strong><code>next()</code></strong>:</p><pre><code class="javascript">expect(clone.next(false).value).toEqual( put(fetchFailedAction()) );      </code></pre><p><br></p><h2 id="Saga-vs-Observables"><a href="#Saga-vs-Observables" class="headerlink" title="Saga vs Observables"></a>Saga vs Observables</h2><p>Redux saga is not the only solution to our apps which may have complex control flows, they are other helpful tools providing different trade-offs which can also resolve the async problems. Here are some good <a href="https://hackmd.io/s/H1xLHUQ8e" target="_blank" rel="noopener">code snippets</a> of saga vs observables that can open your mind :D</p><p><br><br><br></p><h2 id="References"><a href="#References" class="headerlink" title="References:"></a>References:</h2><p><a href="https://redux-saga.js.org/" target="_blank" rel="noopener">https://redux-saga.js.org/</a><br><a href="https://stackoverflow.com/questions/25098066/what-is-callback-hell-and-how-and-why-rx-solves-it" target="_blank" rel="noopener">https://stackoverflow.com/questions/25098066/what-is-callback-hell-and-how-and-why-rx-solves-it</a><br><a href="https://redux.js.org/advanced/middleware" target="_blank" rel="noopener">https://redux.js.org/advanced/middleware</a><br><a href="https://pub.dartlang.org/packages/redux_thunk" target="_blank" rel="noopener">https://pub.dartlang.org/packages/redux_thunk</a><br><a href="https://codeburst.io/how-i-test-redux-saga-fcc425cda018" target="_blank" rel="noopener">https://codeburst.io/how-i-test-redux-saga-fcc425cda018</a><br><a href="https://engineering.universe.com/what-is-redux-saga-c1252fc2f4d1" target="_blank" rel="noopener">https://engineering.universe.com/what-is-redux-saga-c1252fc2f4d1</a><br><a href="https://www.sitepoint.com/redux-without-react-state-management-vanilla-javascript/" target="_blank" rel="noopener">https://www.sitepoint.com/redux-without-react-state-management-vanilla-javascript/</a><br><a href="https://redux.js.org/introduction/getting-started" target="_blank" rel="noopener">https://redux.js.org/introduction/getting-started</a><br><a href="https://blog.logrocket.com/understanding-redux-saga-from-action-creators-to-sagas-2587298b5e71" target="_blank" rel="noopener">https://blog.logrocket.com/understanding-redux-saga-from-action-creators-to-sagas-2587298b5e71</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;If you are quite experienced with redux, which is a predictable state container for JavaScript applications (&lt;strong&gt;Note:&lt;/strong&gt; even thouth React and Redux is a popular combination to build fast and powerful apps, Redux is not necessarily combined with React), you are definitely feeling comfortable with its powerful store which manages all the global states and provides much cleaner logic flows to change them. If you are new to redux, &lt;a href=&quot;https://redux.js.org/introduction/getting-started&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;here&lt;/a&gt; is the guide to dive before we start our topic.&lt;/p&gt;
    
    </summary>
    
    
      <category term="Redux" scheme="https://ohyoooo.github.io/tags/Redux/"/>
    
      <category term="Saga" scheme="https://ohyoooo.github.io/tags/Saga/"/>
    
      <category term="React" scheme="https://ohyoooo.github.io/tags/React/"/>
    
  </entry>
  
</feed>
